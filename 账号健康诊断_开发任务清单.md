# FlowX「账号健康诊断」开发任务清单（可执行版）

> 配套文档：账号健康诊断_PRD.md（v0.2 决策锁定）
> 复用基座：已合并的 `账号分析` 模块（采集管线 / AnalyticsStore / AnalyticsPanel）
> 范围：砍 Python 栈，全部 TS 原生；4 阶段（P1–P4），总估时 8–11 周
> 任务粒度：每个任务可独立提测；文件指向 `flowx-desktop/` 下相对路径

---

## 总览

| 阶段 | 估时 | 任务数 | 关键产出 | 上线价值 |
|------|------|-------|---------|---------|
| P1 数据管道 | 2–3 周 | 6 | 账号级指标入库 + 健康历史表 + 平台内基准 | 能拉出每个号的原始指标快照 |
| P2 评分引擎 | 2 周 | 4 | 五维评分 + AHP 配置 + 评分卡/雷达 UI | 用户看到 0–100 分 + 红绿灯 |
| P3 异常检测 | 2–3 周 | 5 | 异常检测 + 预警规则 + 应用内触达 | 风险提前 48–72h 预警 |
| P4 可视化与批量 | 2–3 周 | 4 | 报告页 + 批量诊断 + 独立导航入口 + 周报导出 | 小白可用 + 矩阵批量体检 |
| 横切 | 全程 | 3 | 隐私合规 / 单测 / 灰度 | 不踩红线、质量兜底 |

依赖链：`P1 → P2 → P3 → P4`；横切任务贯穿。

---

## P1 数据管道（2–3 周）

### T1.1 账号级健康指标类型定义
- **文件**：`src/types/index.ts`（追加到已合并的 analytics 类型块之后）
- **内容**：新增 `AccountHealthSnapshot`（账号维度快照）、`HealthDimensionKey`（5 常量枚举）、`HealthRawMetric`（原始指标：fanCount/fanGrowth/dropRate/interactionRatio/publishCount/loginFreq/completenessScore/violationPilot...）、`PlatformBenchmark`（分平台分位 Xmin/Xmax）。
- **验收**：`vue-tsc --noEmit` 通过；类型可从 Collector 输出到 Store 全程贯通。
- **依赖**：无。

### T1.2 采集扩展基类（复用 + 账号级指标）
- **文件**：`src/main/services/analytics/BaseCollector.ts`；各 `platforms/*.ts`（Douyin/Kuaishou/Xiaohongshu/WechatChannels）
- **内容**：在现有 `safeEval` 采集粉丝/获赞/评论/完播率基础上，新增账号级指标采集钩子：① 基础信息完整度（头像/昵称/简介/认证/运营天数）；② 活跃度（近 30 天发布数、登录频率、互动频率，部分读本地行为日志）；③ 粉丝增长（增长率、掉粉率）。
- **验收**：4 平台账号可产出完整 `AccountHealthSnapshot`（含原始值）；单账号采集 < 30s。
- **依赖**：T1.1。
- **注意**：**不引入 Python 第二栈**，全部在现有 webview 登录态内扩展；遵守 Rate Limit。

### T1.3 违规维度小范围试点采集（决策 2）
- **文件**：`src/main/services/analytics/platforms/*.ts`（新增 `collectViolationPilot()`）
- **内容**：仅采「异常登录设备 / 处罚等级」——**若后台页面可稳定采集**则落库；不可采的账号返回 `available:false`，评分引擎对该维度标记「数据不足」而非给 0 分。
- **验收**：可采平台能产出 pilot 字段；不可采平台不报错、不阻塞主流程。
- **依赖**：T1.2。
- **风险**：后台页面结构易变；若试点发现普遍不可采，整体违规维度降级为「数据不足」占位（不阻塞 P2）。

### T1.4 健康历史存储
- **文件**：`src/main/services/analytics/AnalyticsStore.ts`
- **内容**：新增 `health_history` 表（账号 ID + 时间戳 + 5 维原始值 + 快照 JSON），保留 ≥180 天；新增写入/按账号按天查询 API，复用现有 SQLite + `autoCollectIntervalHours:6` 调度。
- **验收**：快照可写可读；历史 ≥180 天的不被清理。
- **依赖**：T1.1、T1.2。

### T1.5 评分输入聚合服务
- **文件**：`src/main/services/analytics/AnalyticsService.ts`（新增 `buildHealthSnapshot(accountId)`）
- **内容**：把 T1.2/T1.3 各平台原始指标聚合成统一 `AccountHealthSnapshot`；僵尸粉比例用「掉粉率 + 互动/粉丝比」代理计算并打 `estimated:true` 标（决策 3）。
- **验收**：输出结构与 T1.1 类型一致；代理指标带估算标注。
- **依赖**：T1.2、T1.3。

### T1.6 平台内基准统计（决策 1）
- **文件**：`src/main/services/analytics/AnalyticsStore.ts` + `AnalyticsService.ts`
- **内容**：实现 Xmax/Xmin = 平台内同类型账号快照的分位统计（按平台 + 按账号层级如粉丝量级分层），支撑 P2 归一化。
- **验收**：给定平台+层级，能返回各指标分位基准；样本不足时回退到全局平台基准并标注。
- **依赖**：T1.4（需历史数据积累；MVP 先用冷启动默认基准，随采集自动校准）。

---

## P2 评分引擎（2 周）

### T2.1 五维加权评分引擎
- **文件**：`src/main/services/analytics/HealthScoreEngine.ts`（新建）
- **内容**：Min-Max 归一化到 [0,100]（正向/负向指标公式）；权重初值 基础 20% / 活跃 20% / 违规 30% / 粉丝质量 15% / 内容表现 15%；5 级分级（🟢优秀/🟢良好/🟡一般/🟠预警/🔴高危）；违规维度「数据不足」时按剩余 4 维加权重算并标注口径。
- **验收**：输入 `AccountHealthSnapshot` 正确输出 0–100 分 + 等级 + 各维子分；权重可在配置页调整（见 T2.2）。
- **依赖**：T1.5、T1.6。

### T2.2 AHP 权重配置
- **文件**：`src/main/services/analytics/AnalyticsStore.ts`（config 表）+ `src/renderer/stores/analytics.ts`
- **内容**：权重可配置（按平台/行业动态调整，如强监管领域违规权重可提到 40%）；配置持久化 + 渲染端读写。
- **验收**：改权重后重新评分结果随之变化；配置跨重启保留。
- **依赖**：T2.1。

### T2.3 总评分卡 + 雷达图 UI
- **文件**：`src/renderer/pages/HealthDiagnosisPanel.vue`（新建，复用 `AnalyticsPanel.vue` 容器模板）；图表 `ECharts 5`
- **内容**：顶部总评分卡（颜色圆环 + 5 级标签）→ 中部左五维雷达 → 底部问题清单雏形；评分卡用 HTML+CSS 圆环。
- **验收**：传入快照能渲染评分卡 + 雷达；首屏 < 1.5s（读缓存历史分）。
- **依赖**：T2.1。

### T2.4 小白解读器翻译表
- **文件**：`src/renderer/constants/healthDictionary.ts`（新建）
- **内容**：专业指标 → 人话 + 红绿灯 emoji 映射（复用 PRD §5.4 翻译表）；问题清单每项含「当前值 → 建议值 → 操作入口」。
- **验收**：任一扣分项能渲染人话解读。
- **依赖**：T2.1。

---

## P3 异常检测（2–3 周）

### T3.1 单变量异常检测
- **文件**：`src/main/services/analytics/HealthAnomalyDetector.ts`（新建）
- **内容**：Z-Score（|Z|>3）、移动平均+残差（窗口 7–14 天，阈值 2–3σ）、STL 季节性分解（互动周期）实现于 TS（mathjs/jstat 或自实现）。
- **验收**：在合成数据上能识别粉丝骤降/互动率暴跌拐点。
- **依赖**：T1.4（需历史序列）。

### T3.2 多变量近似（Isolation Forest 等价）
- **文件**：同 T3.1
- **内容**：用「多变量 Z-Score + 规则组合」近似孤立森林；MVP 不引 Python（决策：Isolation Forest 用近似兜底，V2 再评估轻量 Python 子进程）。
- **验收**：多指标联合异常能触发（如掉粉+互动双降）。
- **依赖**：T3.1。

### T3.3 趋势线事件标注
- **文件**：`HealthDiagnosisPanel.vue` + 趋势组件
- **内容**：近 30 天健康度趋势线标注关键事件（违规、异常登录、掉粉拐点），数据来自 `health_history` + 异常检测结果。
- **验收**：事件点在趋势线上正确标注 + hover 说明。
- **依赖**：T3.1、T3.2。

### T3.4 预警规则与降误报
- **文件**：`HealthAnomalyDetector.ts` + `AnalyticsStore.ts`（预警配置）
- **内容**：预警规则配置（灵敏度 contamination 2%–5%、持续性连续 ≥3 点异常、集成投票 2/3 同意）；**MVP 宁可漏报不可误报**。
- **验收**：单点抖动不误报；连续异常才触发；误报率 ≤10%（健康指标）。
- **依赖**：T3.1、T3.2。

### T3.5 应用内触达（决策 5）
- **文件**：`src/renderer/stores/workspace.ts`（通知）、账号列表角标、`src/renderer/App.vue` 通知区
- **内容**：预警 → 账号列表红点角标 + 系统通知；一键跳转该账号诊断详情与整改建议。**MVP 仅应用内**，短信/邮件/IM 列入 V2。
- **验收**：触发预警后角标 + 通知出现，点击直达诊断页。
- **依赖**：T3.4。

---

## P4 可视化与批量（2–3 周）

### T4.1 ECharts 完整诊断报告页
- **文件**：`HealthDiagnosisPanel.vue`（补全）
- **内容**：顶部评分卡 → 左雷达 + 右趋势 → 下部问题清单（按严重度排序，含扣分→当前值→建议值→入口）→ 底部一键解读。布局对齐 PRD §5.4。
- **验收**：用户看到完整四段式报告；一键解读展开人话。
- **依赖**：T2.3、T3.3、T2.4。

### T4.2 批量诊断入口
- **文件**：`src/renderer/pages/AccountPanel.vue`（或新建批量入口）+ `AnalyticsService.ts`
- **内容**：一键扫描全部绑定账号，列表按风险等级排序；批量 < 5min/100 账号。
- **验收**：点击后全账号出健康分并排序；性能达标。
- **依赖**：T2.1、T1.5。

### T4.3 独立导航入口「账号诊断分析」（决策 4）
- **文件**：`src/renderer/App.vue`（`primaryNav` 加 entry）、`src/renderer/router/index.ts`（加 `/health-diagnosis` 路由）、`src/renderer/stores/workspace.ts`（`ROUTE_META` 补 `/health-diagnosis`）
- **内容**：新增一级菜单「账号诊断分析」，与「账号分析」同级、独立打开，直达 T4.1 报告页。
- **验收**：左侧栏出现独立入口，点击打开诊断页，不与账号分析混淆。
- **依赖**：T4.1。

### T4.4 健康周报导出
- **文件**：`src/renderer/pages/HealthDiagnosisPanel.vue` + `src/main`（导出 IPC）
- **内容**：导出账号健康周报（Markdown/PDF），供管理者向上汇报。
- **验收**：勾选账号能导出含评分卡+趋势+问题清单的周报。
- **依赖**：T4.1。

---

## 横切任务（贯穿全程）

### X1 隐私与平台合规
- **文件**：延续现有 `setupImageAntiHotlink`/加密约定；涉及敏感数据（手机号、Cookie）本地加密、不上云。
- **验收**：合规自检通过；采集频率控制在低峰、遵守各平台 Rate Limit / robots。
- **依赖**：无。

### X2 单元测试与集成测试
- **文件**：`src/main/services/analytics/__test__/`（沿用 `queue-test.ts` 模式）
- **内容**：评分引擎数值正确性、异常检测合成数据命中、Collector 失败降级（「数据不足」不崩）。
- **验收**：核心算法单测覆盖；CI 通过。
- **依赖**：各阶段任务。

### X3 灰度与验收
- **内容**：P1/P2 内部灰度（自身矩阵账号）；P3 起小范围公测；按 PRD §6 指标体系验收（覆盖率 ≥80%、误报率 ≤10%、首屏 <1.5s）。
- **依赖**：P1–P4。

---

## 关键文件路径速查（复用基座）
- 采集：`src/main/services/analytics/platforms/*.ts`、`BaseCollector.ts`
- 存储/调度：`src/main/services/analytics/AnalyticsStore.ts`（SQLite + 6h 定时）
- 聚合：`src/main/services/analytics/AnalyticsService.ts`
- IPC：`src/main/ipc/analytics.ts` · Preload：`src/preload/index.ts`（`analytics` 命名空间）
- 渲染 store：`src/renderer/stores/analytics.ts`
- 面板模板：`src/renderer/pages/AnalyticsPanel.vue`
- 导航/路由：`src/renderer/App.vue`（`primaryNav`）、`src/renderer/router/index.ts`、`src/renderer/stores/workspace.ts`（`ROUTE_META`）
- 类型：`src/types/index.ts`（analytics 类型块）

## 提交与推送约定（沿用本仓库排障流程）
- 每次提交用「指定文件 `git add`」，**排除 `flowx-desktop/nul` 垃圾文件**（否则 `git add -A` 必失败）。
- 本沙箱 GCM 无法取 Windows 凭据令牌 → `git push` 报 401；最终推送需你本机终端执行或提供 GitHub PAT。

# 功能规格（PRD）：日志管理 - 日志级别筛选

> 版本：v0.1 ｜ 状态：待评审 ｜ 提出方：产品 ｜ 归属模块：系统配置 / 日志管理
> 关联页面：`flowx-desktop/src/renderer/pages/LogPanel.vue`

---

## 1. 背景与价值（Why）

**用户痛点**
日志管理页以纯文本方式展示日志（主日志 / 发布日志），单次最多 2000 行。当前唯一的收窄手段是"关键词搜索"。当用户（运营/技术支持/研发）想排查**警告（Warning）**类问题时，必须在几百上千行里肉眼扫黄字，或赌关键词能命中——效率低、易漏看。

**业务目标**
让"按级别看日志"成为一键操作：用户勾选"警告"即可直接过滤出所有警告行，无需滚动、无需猜关键词。这是日志定位体验从"搜索"迈向"分类浏览"的关键一步。

**本次不做的事（先解决 80% 场景）**：不做后端结构化索引、不做级别统计、不做 debug 级别。

---

## 2. 用户故事（User Stories）

| # | 角色 | 我希望 | 以便 |
|---|------|--------|------|
| US-1 | 技术支持 | 勾选"警告"级别 | 快速把所有警告日志聚到一起发给研发，不用在长文里摘 |
| US-2 | 运营 | 同时勾选"警告+错误" | 一眼看清某次发布里有哪些异常，不被大量 info 淹没 |
| US-3 | 研发 | 在已选级别的基础上再叠加关键词 | 在"错误"范围内精准定位某个任务/账号的问题 |
| US-4 | 所有用户 | 清空级别筛选 | 一键回到"全部级别"的原始视图，零心智负担 |

---

## 3. 功能需求（Functional Requirements）

### FR-1 级别多选筛选器
- 在日志查看工具栏新增一个 **多选下拉**，选项固定为：`信息(info)`、`警告(warn)`、`错误(error)`。
- 下拉默认**未选中任何项 = 显示全部级别**（保持当前行为不变，零破坏）。
- 选中 1~N 个级别后，日志区**仅显示命中这些级别的行**。
- 下拉支持清空（clearable），清空即恢复"全部级别"。

### FR-2 与现有关键词搜索叠加（AND）
- 级别筛选在关键词搜索**之后**生效：同一行必须**同时满足**"含关键词"且"级别 ∈ 已选集合"才展示。
- 与现有"按日期选文件""行数上限"自然叠加，互不冲突。

### FR-3 即时过滤（无重新请求）
- 过滤在已加载的日志文本上**纯前端即时计算**，切换选项无需重新读文件、无网络/磁盘 IO。
- 复用现有 `detectLevel(line)` 给每行判定级别（见技术约束）。

### FR-4 视觉一致性
- 下拉选项 / 选中标签按级别着色，与日志正文现有配色对齐：
  - 信息 = 绿 `#6a9955`
  - 警告 = 黄 `#e5c07b`
  - 错误 = 红 `#f48771`
- 过滤后命中行的着色规则不变（仍按 `.lvl-info / .lvl-warn / .lvl-error`）。

### FR-5 空结果态
- 当"级别 + 关键词 + 日期 + 行数"组合下无匹配行时，日志区显示明确文案：`（当前筛选条件下暂无日志）`，而非空白或旧文案。

---

## 4. 交互与 UI 规格

**位置**：日志查看工具栏 `.toolbar-right` 内，顺序建议：`日期选择` → `行数上限` → **`级别多选`** → `关键词搜索` → `刷新/导出`（级别是强过滤维度，放在关键词左侧更顺手）。

**控件形态**：`el-select` + `multiple` + `clearable` + `collapse-tags`（选项多时折叠为 "+N"），`placeholder="全部级别"`，宽度约 150px。

**标签着色**：选项 `<el-option>` 文字按级别上色；选中后 tag 同步着色，提升"当前在看什么级别"的扫读效率。

**示例交互流（US-1）**：
1. 打开日志管理 → 默认显示全部级别（行为不变）。
2. 点击级别下拉 → 勾选"警告" → 日志区即时只剩黄色警告行。
3. 再叠加关键词"token" → 仅剩"警告且含 token"的行。
4. 点清空 → 恢复全部。

---

## 5. 非目标（Non-goals）

- ❌ 不修改日志落盘级别或存储结构。
- ❌ 不在主进程/D B 层建立结构化级别索引（文本日志无结构化字段，本期纯前端过滤足够）。
- ❌ 不做按级别的数量统计/占比（概览卡片增强，本期不做）。
- ❌ 不做级别筛选的持久化（刷新页面恢复"全部级别"默认态，符合日志查看器的临时探索性质）。
- ❌ 不引入 debug 级别（见 §6 约束）。

---

## 6. 边界与限制（Constraints）

- **主日志不记录 debug**：源码 `log.transports.file.level = 'info'`（`src/main/utils/logger.ts:64`），debug 仅存在于发布日志文件。本期级别范围已排除 debug，避免"主日志筛 debug 永远为空"的困惑。
- **级别判定是启发式**：`detectLevel()` 依据行内 `ERROR/WARN` 等关键字判定。普通日志正文若恰好出现 "ERROR" 字样（非级别 token），会被误判为错误级——但日志格式强制带 `[LEVEL]` token 且查看器面向内部人员，误判可接受，不在本期修正。
- **性能**：纯前端 `computed` 过滤，2000 行内耗时可忽略（<50ms），无性能回归风险。

---

## 7. 指标（Metrics）

| 类型 | 指标 | 基线 / 目标 |
|------|------|------------|
| 北极星 | 用户定位目标级别日志的平均操作步数 | 从"滚动/猜关键词"降至 **1 步（勾选）** |
| 驱动 | 级别筛选器使用率（若该页有 UI 埋点） | 上线后 4 周内 ≥ 30% 的日志查看会话使用过筛选 |
| 健康 | 过滤操作耗时 | 万行内 P95 < 50ms，无卡顿 |
| 健康 | 回归 | 关键词搜索 / 导出 / 日期切换功能不受影响 |

> 注：当前桌面端未强制埋点，北极星/驱动指标以发布后抽样回访 + 用户反馈定性评估为主。

---

## 8. 风险与缓解（Risks）

| 风险 | 影响 | 缓解 |
|------|------|------|
| 启发式级别判定误判 | 个别行级别标错，过滤漏/多 | 依赖 `[LEVEL]` token 格式；面向内部用户，接受偏差；后续可升级为解析结构化字段 |
| 与关键词叠加后空结果 | 用户以为"没日志了" | FR-5 明确空态文案，区分"真没日志"与"被筛掉" |
| 多选下拉选项少、价值感弱 | 用户觉得"没必要" | 默认全显、零学习成本；着色标签强化"在看什么"的感知 |

---

## 9. 验收标准（Acceptance Criteria）

- [ ] 工具栏出现"级别"多选下拉，选项为 信息 / 警告 / 错误，默认空（=全部）。
- [ ] 仅勾选"警告"→ 日志区只显示 `detectLevel=warn` 的行，且保持黄色着色。
- [ ] 勾选"警告+错误"→ 显示两类行；勾选全部三项 = 显示全部（行为同默认可选全部）。
- [ ] 级别筛选与关键词搜索**叠加生效（AND）**，与日期/行数上限不冲突。
- [ ] 清空下拉 → 恢复"全部级别"。
- [ ] 无匹配时显示 `（当前筛选条件下暂无日志）`。
- [ ] 不新增任何 IPC / 主进程改动；`vue-tsc --noEmit` 通过。
- [ ] 现有"关键词搜索 / 刷新 / 导出 / 清空内存日志"功能行为不变。

---

## 10. 范围决策记录（Reviewer Notes）

- **筛选交互**：多选下拉（与账号面板"平台/分类"多选风格统一）。
- **级别范围**：信息 / 警告 / 错误（排除 debug，规避主日志无 debug 的空结果歧义）。
- **实现路径**：纯前端 `computed` 复用 `detectLevel()`，零后端改动 —— 已与源码核对可行。

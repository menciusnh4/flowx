# 账号分析功能设计方案

> 数据诊断 · 归因分析 · 优化建议 · 对标分析

**日期**：2026-07-24 &nbsp;|&nbsp; **版本**：1.0 &nbsp;|&nbsp; **标签**：第五阶段核心功能

---

## 目录

1. [功能概述](#1-功能概述)
2. [竞品调研与定位差异](#2-竞品调研与定位差异)
3. [数据模型设计](#3-数据模型设计)
4. [整体架构设计](#4-整体架构设计)
5. [数据采集层](#5-数据采集层)
6. [诊断引擎](#6-诊断引擎)
7. [对标分析模块](#7-对标分析模块)
8. [前端页面设计](#8-前端页面设计)
9. [IPC 通道设计](#9-ipc-通道设计)
10. [存储方案](#10-存储方案)
11. [技术坑点与解决方案](#11-技术坑点与解决方案)
12. [实施计划](#12-实施计划)

---

## 1. 功能概述

账号分析功能的核心不是数据展示，而是 **"诊断问题 → 给出建议 → 指导行动"** 的闭环。基于 FlowX 已有的 CDP 浏览器自动化能力，直接从各平台创作者后台采集精准的账号数据，结合规则引擎和 LLM 生成可执行的优化建议。

> 💡 **核心价值**
> - **对用户**：不仅知道数据差，还知道为什么差、怎么改
> - **对产品**：从"发布工具"升级为"运营助手"，提升用户粘性和付费意愿
> - **对技术**：复用已有 CDP + 平台适配器基础设施，数据采集成本低

### 1.1 功能模块组成

| 模块 | 说明 |
|------|------|
| 📊 **数据概览** | 账号核心指标看板、趋势图、健康度评分 |
| 🎯 **作品诊断** | 单作品数据分析 + 问题定位 + 优化建议 |
| 🆚 **对标分析** | 多账号对比、竞品监控、差距分析 |
| 📈 **趋势洞察** | 粉丝增长、互动率、完播率等指标趋势 |
| 📝 **周期报告** | 周/月自动生成运营报告（后续版本） |

### 1.2 与现有功能的关系

**图 1：账号分析在 FlowX 体系中的位置**

```
┌─────────────────────────────────────────────────────────────┐
│                        FlowX 桌面端                          │
├──────────────┬───────────────┬──────────────┬───────────────┤
│   账号管理    │    内容发布    │   浏览器      │   草稿箱       │
│  (已实现)    │   (已实现)    │  (已实现)    │  (已实现)     │
└──────────────┴───────────────┴──────────────┴───────────────┘
                              ▲
                              │ 复用基础设施
                              │
┌─────────────────────────────┴───────────────────────────────┐
│                       账号分析（新增）                        │
├──────────────┬───────────────┬──────────────────────────────┤
│   数据概览    │   作品诊断     │        对标分析              │
└──────────────┴───────────────┴──────────────────────────────┘
         ▲                ▲                  ▲
         │                │                  │
         └────────────────┴──────────────────┘
                          │
              ┌───────────┴───────────┐
              │  AccountService 复用    │
              │  · partition 隔离       │
              │  · cookie 注入          │
              │  · 隐藏窗口模式         │
              │  · CDP / evalJS         │
              └───────────────────────┘
```

### 1.3 数据来源与优势

| 数据来源 | 精度 | 指标丰富度 | 实现方式 |
|---------|------|-----------|---------|
| **创作者后台数据**（核心） | 100% 精确 | 高（完播率/流量来源/画像） | CDP 登录态 + DOM 提取 |
| **公开主页数据**（对标用） | 精确但维度少 | 低（点赞/评论/收藏） | 公开页面 DOM 提取 |
| **CDP 视觉分析**（护城河） | 自主分析 | 独特（封面质量/高光时刻） | 截图 + 视觉模型 |

---

## 2. 竞品调研与定位差异

### 2.1 商业竞品功能矩阵

| 功能模块 | 蝉妈妈 | 飞瓜 | 灰豚 | 新榜 | FlowX |
|---------|:-----:|:----:|:----:|:----:|:-----:|
| 账号概览（粉丝/点赞/作品数） | ✅ | ✅ | ✅ | ✅ | ✅ |
| 作品列表 + 数据指标 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 作品数据趋势图 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 对标账号管理 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 竞品动态监控/预警 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 粉丝画像分析 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 流量来源分析 | ✅ | ✅ | - | - | ✅ |
| 完播率/留存曲线 | ✅ | ✅ | - | ✅ | ✅ |
| 爆款视频拆解 | ✅ | ✅ | ✅ | - | ✅ |
| 评论词云/情感分析 | ✅ | - | ✅ | - | 🔵 |
| 热门话题/标签分析 | ✅ | ✅ | ✅ | ✅ | 🔵 |
| 账号榜单/达人搜索 | ✅ | ✅ | ✅ | ✅ | - |
| **本地数据存储（隐私安全）** | - | - | - | - | ✅ |
| **CDP 视觉分析（封面/帧）** | - | - | - | - | ✅ |
| **与发布流程无缝衔接** | - | - | - | - | ✅ |

> 🔵 = 后续版本支持 &nbsp;&nbsp; ✅ = 核心支持 &nbsp;&nbsp; - = 不支持

### 2.2 核心定位差异

| 维度 | SaaS 竞品（蝉妈妈/飞瓜） | FlowX 账号分析 |
|------|-------------------------|----------------|
| **数据来源** | 公开数据爬取 + 估算模型 | 用户自有账号的后台精准数据 |
| **数据精度** | 估算值，有误差（尤其是完播率等非公开指标） | 100% 精确（创作者后台原始数据） |
| **数据深度** | 表面指标（点赞/评论/播放/粉丝） | 完整指标（完播率/流量来源/粉丝画像/留存曲线） |
| **使用方式** | 云端 SaaS，按账号/按功能付费 | 本地桌面端，一次拥有，账号不限 |
| **核心能力** | 竞品监控、榜单发现、达人搜索 | 自我诊断、优化建议、行动指导 |
| **隐私性** | 数据在第三方服务器 | 全部本地存储，不上传云端 |
| **独特优势** | 数据量大、榜单全 | 数据精准、隐私安全、与发布闭环 |

### 2.3 开源项目参考

| 项目 | 技术栈 | 可借鉴点 |
|------|--------|---------|
| **MediaCrawler** (Python + Playwright) | Playwright 爬虫 + FastAPI + Vue | 平台适配器模式、多存储引擎、词云生成、断点续爬 |
| **MultiPost** (浏览器扩展 + TS) | Chrome Extension + TS | 双 API 模式、Markdown 编辑器、国际化 |
| **Metabase / Superset** | 通用 BI 工具 | 数据可视化交互设计、看板配置化 |

---

## 3. 数据模型设计

### 3.1 作品基础信息（WorkItem）

```typescript
/**
 * 作品基础信息
 */
export interface WorkItem {
  /** 作品唯一 ID（平台内部 ID） */
  id: string;
  /** 所属平台 */
  platform: PlatformType;
  /** 所属账号 ID（系统内部 ID） */
  accountId: string;
  /** 平台账号 ID（抖音号/小红书号/快手号等，平台原生标识） */
  platformAccountId?: string;
  /** 作品标题 */
  title: string;
  /** 封面图 URL（可选） */
  coverUrl?: string;
  /** 内容类型 */
  contentType: ContentType;
  /** 发布时间戳（毫秒） */
  publishTime: number;
  /** 作品详情页 URL（用于跳转） */
  detailUrl?: string;
  /** 作品时长（秒，视频类） */
  duration?: number;
  /** 首次采集时间 */
  firstCollectedAt: number;
  /** 最后更新时间 */
  lastUpdatedAt: number;
}
```

### 3.2 作品数据指标（WorkMetrics）

```typescript
/**
 * 作品数据指标
 * 每次采集生成一条快照，支持时间序列分析
 */
export interface WorkMetrics {
  /** 快照唯一 ID */
  id: string;
  /** 关联作品 ID */
  workId: string;
  /** 所属账号 ID */
  accountId: string;
  /** 平台账号 ID（抖音号/小红书号等，平台原生标识） */
  platformAccountId?: string;
  /** 所属平台 */
  platform: PlatformType;
  /** 播放量/阅读量/浏览量 */
  views: number;
  /** 点赞数 */
  likes: number;
  /** 评论数 */
  comments: number;
  /** 收藏数 */
  favorites: number;
  /** 转发/分享数 */
  shares: number;
  /** 完播率（百分比，0-100，视频平台特有） */
  completionRate?: number;
  /** 平均播放时长（秒） */
  avgPlayDuration?: number;
  /** 新增粉丝数（该作品带来的） */
  newFans?: number;
  /** 曝光量/展现量 */
  impressions?: number;
  /** 点击率 = 播放/曝光（百分比） */
  clickRate?: number;
  /** 互动率 = (赞+评+藏+转)/播放（百分比） */
  interactionRate?: number;
  /** 转粉率 = 新增粉丝/播放（百分比） */
  fanConversionRate?: number;
  /** 采集时间戳 */
  collectedAt: number;
}
```

### 3.3 账号统计快照（AccountStatsSnapshot）

```typescript
/**
 * 账号时间序列统计快照
 * 按天聚合，用于趋势分析
 */
export interface AccountStatsSnapshot {
  /** 快照唯一 ID */
  id: string;
  /** 账号 ID */
  accountId: string;
  /** 统计日期（YYYY-MM-DD） */
  date: string;
  /** 粉丝总数（当日末） */
  fansCount: number;
  /** 关注数 */
  followCount?: number;
  /** 总获赞数 */
  totalLikeCount?: number;
  /** 当日新增粉丝 */
  newFans?: number;
  /** 当日总播放/阅读量 */
  totalViews?: number;
  /** 当日总互动数（赞+评+藏+转） */
  totalInteractions?: number;
  /** 当日发布作品数 */
  worksPublished?: number;
  /** 当日平均完播率 */
  avgCompletionRate?: number;
  /** 当日平均互动率 */
  avgInteractionRate?: number;
  /** 采集时间戳 */
  collectedAt: number;
}
```

### 3.4 作品诊断结果（WorkDiagnosis）

```typescript
/**
 * 单作品诊断结果
 */
export interface WorkDiagnosis {
  /** 关联作品 ID */
  workId: string;
  /** 综合评分（0-100） */
  overallScore: number;
  /** 各维度评分 */
  dimensionScores: {
    /** 钩子/开头质量 */
    hook: number;
    /** 内容质量 */
    content: number;
    /** 互动转化 */
    engagement: number;
    /** 转粉效率 */
    conversion: number;
    /** 封面/标题点击率 */
    clickThrough: number;
  };
  /** 问题列表 */
  issues: Array<{
    /** 问题类型 */
    type: 'hook' | 'content' | 'engagement' | 'conversion' | 'clickThrough';
    /** 严重程度 */
    severity: 'low' | 'medium' | 'high';
    /** 问题标题 */
    title: string;
    /** 详细描述 */
    description: string;
    /** 关联指标 */
    metric?: {
      name: string;
      value: number;
      benchmark: number;
      unit?: string;
    };
  }>;
  /** 优化建议列表 */
  suggestions: Array<{
    /** 优先级 */
    priority: 'high' | 'medium' | 'low';
    /** 建议标题 */
    title: string;
    /** 详细说明 */
    detail: string;
    /** 可执行的具体步骤 */
    actionableStep?: string;
  }>;
  /** 生成时间 */
  generatedAt: number;
  /** 诊断引擎版本 */
  engineVersion: string;
}
```

### 3.5 对标账号（BenchmarkAccount）

```typescript
/**
 * 对标账号配置
 */
export interface BenchmarkAccount {
  /** 本地唯一 ID */
  id: string;
  /** 所属主账号 ID（谁的对标） */
  ownerAccountId: string;
  /** 对标账号平台 */
  platform: PlatformType;
  /** 对标账号的平台主页 URL */
  profileUrl: string;
  /** 对标账号名称 */
  name: string;
  /** 对标账号头像 URL */
  avatar?: string;
  /** 对标账号的平台用户 ID */
  platformUserId?: string;
  /** 对标类型 */
  type: 'direct' | 'potential' | 'benchmark';
  /** 自定义备注 */
  remark?: string;
  /** 最近一次采集时间 */
  lastCollectedAt?: number;
  /** 创建时间 */
  createdAt: number;
  /** 启用状态 */
  enabled: boolean;
}

/**
 * 对标账号数据快照（公开数据）
 */
export interface BenchmarkSnapshot {
  id: string;
  benchmarkId: string;
  fansCount: number;
  worksCount: number;
  totalLikes?: number;
  /** 最近 N 条作品的公开数据 */
  recentWorks: Array<{
    workId: string;
    title: string;
    coverUrl?: string;
    publishTime: number;
    likes: number;
    comments: number;
    favorites?: number;
    views?: number;
  }>;
  collectedAt: number;
}
```

### 3.6 行业基准数据（BenchmarkData）

```typescript
/**
 * 行业基准数据（用于对比诊断）
 * 按平台 + 粉丝量级 + 内容类型分类
 */
export interface IndustryBenchmark {
  platform: PlatformType;
  /** 粉丝量级区间 */
  fanRange: '0-1k' | '1k-10k' | '10k-100k' | '100k-1M' | '1M+';
  /** 内容类型 */
  contentType: ContentType;
  /** 完播率基准（百分比） */
  completionRate: {
    excellent: number;  // 前 20%
    good: number;       // 前 50%
    warning: number;    // 后 30% 警戒线
  };
  /** 互动率基准 */
  interactionRate: {
    excellent: number;
    good: number;
    warning: number;
  };
  /** 点击率基准 */
  clickRate: {
    excellent: number;
    good: number;
    warning: number;
  };
  /** 转粉率基准 */
  fanConversionRate: {
    excellent: number;
    good: number;
    warning: number;
  };
}
```

---

## 4. 整体架构设计

### 4.1 架构分层图

**图 2：账号分析模块架构图**

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Renderer Process (Vue 3)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌───────────┐  │
│  │  Overview.vue│  │  Works.vue   │  │WorkDetail.vue│  │Benchmark  │  │
│  │  数据概览页   │  │  作品列表页   │  │  作品诊断页   │  │ 对标分析页 │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └─────┬─────┘  │
│         │                 │                 │                │         │
│  ┌──────▼─────────────────▼─────────────────▼────────────────▼─────┐   │
│  │                    analyticsStore (Pinia)                        │   │
│  │  数据缓存 · 加载状态 · 分页管理 · 筛选条件 · 诊断结果缓存         │   │
│  └────────────────────────────────┬────────────────────────────────┘   │
└───────────────────────────────────┼────────────────────────────────────┘
                                    │ contextBridge / electronApi
                                    │  (IPC 通道，见 §9)
┌───────────────────────────────────▼────────────────────────────────────┐
│                       Electron Main Process                            │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    AnalyticsService (总调度)                     │   │
│  │  · 采集任务调度（手动/定时/队列）                                 │   │
│  │  · 数据聚合与计算                                                │   │
│  │  · 诊断引擎调用                                                  │   │
│  │  · 对标分析计算                                                  │   │
│  │  · 存储读写封装                                                  │   │
│  └────────────┬─────────────────────────────┬──────────────────────┘   │
│               │                             │                          │
│  ┌────────────▼──────────┐     ┌────────────▼──────────────┐           │
│  │   数据采集层           │     │      诊断引擎              │           │
│  │  (Collector 体系)      │     │  (DiagnosisEngine)        │           │
│  │                       │     │                           │           │
│  │  · BaseCollector      │     │  · 规则引擎 (RuleEngine)   │           │
│  │  · DouyinCollector    │     │  · 行业基准库              │           │
│  │  · XiaohongshuCol.    │     │  · 建议模板库              │           │
│  │  · KuaishouCollector  │     │  · LLM 增强 (可选)         │           │
│  │  · BenchmarkCollector │     │                           │           │
│  └────────────┬──────────┘     └───────────────────────────┘           │
│               │                                                          │
│  ┌────────────▼──────────────────────────────────────────────────────┐  │
│  │        平台基础设施复用（AccountService + PlatformAdapter）        │  │
│  │  · partition 隔离     · cookie 注入     · 隐藏窗口模式             │  │
│  │  · CDP 调试协议       · evalJS 重试     · 导航跟踪器               │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                       数据存储层                                  │   │
│  │  · analyticsStore (electron-store)   — MVP 阶段                 │   │
│  │  · better-sqlite3                    — 数据量大后升级            │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.2 核心设计原则

| 原则 | 描述 |
|------|------|
| **复用优先** | 最大限度复用 AccountService 的 partition/cookie/窗口/CDP 基础设施，不重复造轮子 |
| **平台隔离** | 各平台数据采集逻辑独立到各自 Collector 文件，修改一个平台不影响其他 |
| **采集与分析分离** | 数据采集是一层，诊断分析是另一层，可独立演进和测试 |
| **渐进增强** | 规则引擎先上（快、稳、可解释），LLM 后上（个性化、自然语言） |
| **本地优先** | 所有数据本地存储，不上传云端，保障用户隐私 |
| **增量采集** | 只采集上次之后的新数据，避免全量重复抓取 |

---

## 5. 数据采集层

### 5.1 Collector 体系结构

采用**平台适配器 + 采集器基类**的模式，与现有发布平台适配器思路一致。

```
src/main/services/analytics/
├── AnalyticsService.ts          # 总调度服务
├── collector/
│   ├── BaseCollector.ts         # 采集器基类（窗口管理/导航/通用工具）
│   ├── DouyinCollector.ts       # 抖音数据采集器
│   ├── XiaohongshuCollector.ts  # 小红书数据采集器
│   ├── KuaishouCollector.ts     # 快手数据采集器
│   └── BenchmarkCollector.ts    # 公开页对标数据采集器
├── diagnosis/                   # 诊断引擎（见 §6）
├── benchmark/                   # 对标分析（见 §7）
└── storage/                     # 存储层
    └── AnalyticsStore.ts
```

### 5.2 BaseCollector 基类

```typescript
/**
 * 数据采集器基类
 * 封装窗口创建、cookie 注入、导航等待等通用逻辑
 * 复用 AccountService 的基础设施
 */
abstract class BaseCollector {
  protected platform: PlatformType;
  protected accountId: string;

  /**
   * 创建/复用采集窗口（隐藏模式，复用 partition）
   * 与发布窗口类似，但尺寸更小、默认隐藏
   */
  protected async createCollectWindow(): Promise<BrowserWindow>;

  /**
   * 注入账号 cookies（复用 AccountService.injectAccountCookies）
   */
  protected async injectCookies(win: BrowserWindow): Promise<void>;

  /**
   * 导航到指定 URL 并等待页面稳定
   * 复用 NavigationTracker 机制
   */
  protected async navigateAndWait(win: BrowserWindow, url: string, options?: {
    minStableMs?: number;
    timeoutMs?: number;
  }): Promise<void>;

  /**
   * 安全执行 JS（复用 evalJS 重试逻辑）
   */
  protected async evalJS<T>(win: BrowserWindow, code: string, desc: string): Promise<T>;

  /**
   * 关闭采集窗口
   */
  protected closeWindow(win: BrowserWindow): void;

  // ===== 各平台子类实现 =====

  /** 采集账号概览数据 */
  abstract collectOverview(win: BrowserWindow): Promise<{
    fansCount: number;
    newFans7d?: number;
    totalViews7d?: number;
    // ...
  }>;

  /** 采集作品列表 */
  abstract collectWorksList(win: BrowserWindow, options?: {
    limit?: number;
    cursor?: string;
  }): Promise<{
    works: WorkItem[];
    metrics: Omit<WorkMetrics, 'id' | 'collectedAt'>[];
    hasMore: boolean;
    nextCursor?: string;
  }>;

  /** 采集单作品详细数据 */
  abstract collectWorkDetail(win: BrowserWindow, workId: string): Promise<{
    work: WorkItem;
    metrics: Omit<WorkMetrics, 'id' | 'collectedAt'>;
    trafficSources?: Array<{ source: string; percentage: number }>;
    audienceProfile?: {
      genderRatio?: { male: number; female: number };
      ageDistribution?: Array<{ range: string; percentage: number }>;
    };
    retentionCurve?: Array<{ position: number; retention: number }>;
  }>;
}
```

### 5.3 各平台数据采集路径

| 平台 | 数据中心 URL | 可采集指标 | 优先级 |
|------|-------------|-----------|--------|
| **抖音** | `creator.douyin.com/creator-micro/data` | 完播率、5s 完播率、平均播放时长、曝光、点击率、流量来源、粉丝画像、评论分析 | P0 |
| **小红书** | `creator.xiaohongshu.com/` 创作者中心 | 播放/阅读、点赞、评论、收藏、涨粉、粉丝画像、流量来源 | P0 |
| **快手** | `cp.kuaishou.com/` 创作者服务平台 | 播放、点赞、评论、收藏、完播率、粉丝分析 | P1 |
| **视频号** | `channels.weixin.qq.com/platform/...` | 播放、点赞、评论、收藏、转发、关注 | P1 |
| **知乎** | `zhuanlan.zhihu.com/...` | 阅读、赞同、评论、收藏、喜欢、关注 | P2 |
| **B站** | `member.bilibili.com/` 创作中心 | 播放、点赞、投币、收藏、转发、完播率、粉丝画像 | P2 |

### 5.4 采集策略

| 策略 | 说明 |
|------|------|
| **手动触发** | 用户点击"刷新数据"按钮，立即开始采集 |
| **定时采集** | 可配置频率（每 2/4/8/24 小时），后台自动采集 |
| **增量采集** | 记录上次采集时间和最后一条作品 ID，只采集新发布的作品 |
| **深度采集** | 对高关注的作品（如爆款/诊断为问题的作品）定期刷新详细数据 |
| **队列控制** | 同时最多 N 个采集任务并行（默认 2，避免开太多窗口占资源） |
| **失败重试** | 采集失败自动重试最多 3 次，指数退避 |

### 5.5 采集窗口复用策略

为避免频繁创建销毁 BrowserWindow，采用**窗口池**策略：

1. 每个账号最多复用 1 个采集窗口
2. 采集完成后窗口保持 5 分钟，期间有新任务直接复用
3. 5 分钟空闲后自动关闭窗口，释放资源
4. 窗口复用前检查登录态，过期则重新注入 cookie

---

## 6. 诊断引擎

### 6.1 诊断流程

```
作品原始数据
    ↓
指标计算（互动率/转粉率/点击率等）
    ↓
行业基准对比（按平台+粉丝量级+内容类型）
    ↓
规则匹配（多维度规则库）
    ↓
问题识别（按严重程度排序）
    ↓
建议生成（从建议模板库匹配）
    ↓
综合评分（各维度加权）
    ↓
诊断结果（WorkDiagnosis）
```

### 6.2 诊断维度与规则

#### 6.2.1 钩子质量（Hook）

| 规则 | 触发条件 | 严重程度 | 建议 |
|------|---------|---------|------|
| 完播率偏低 | 完播率 < 行业基准 warning | high/medium | 将最精彩的画面/金句前置到前3秒 |
| 前5秒流失高 | 5秒完播率 < 50% | high | 优化开头，增加悬念或视觉冲击 |
| 平均时长短 | 平均播放时长 < 视频时长 30% | high | 精简内容，加快节奏 |

#### 6.2.2 内容质量（Content）

| 规则 | 触发条件 | 严重程度 | 建议 |
|------|---------|---------|------|
| 完播率一般 | 完播率 在 warning ~ good 之间 | low | 优化内容结构，增加信息密度 |
| 进度条中段流失 | 留存曲线在 30%-70% 区间下降快 | medium | 中段增加转折或干货，避免拖沓 |

#### 6.2.3 互动转化（Engagement）

| 规则 | 触发条件 | 严重程度 | 建议 |
|------|---------|---------|------|
| 互动率偏低 | 互动率 < 行业基准 warning | medium | 增加互动引导，如提问、投票、求赞 |
| 评论率偏低 | 评论/播放 < 0.1% | low | 在内容中设置争议点或讨论话题 |
| 收藏率偏低 | 收藏/播放 < 0.5%（教程类） | medium | 增加实用价值，整理清单/工具/方法 |

#### 6.2.4 转粉效率（Conversion）

| 规则 | 触发条件 | 严重程度 | 建议 |
|------|---------|---------|------|
| 转粉率偏低 | 转粉率 < 行业基准 warning | medium | 结尾增加关注引导，明确关注价值 |
| 高播放低涨粉 | 播放量 Top 10 但涨粉量不在 Top 10 | high | 检查内容是否偏离账号定位，强化人设 |

#### 6.2.5 封面/标题点击率（ClickThrough）

| 规则 | 触发条件 | 严重程度 | 建议 |
|------|---------|---------|------|
| 点击率偏低 | 点击率 < 行业基准 warning | high | 优化封面设计和标题文案，提升点击欲望 |
| 曝光高播放低 | 曝光量高但播放量低 | high | AB 测试不同封面/标题组合 |

### 6.3 综合评分算法

```typescript
function calculateOverallScore(dimensionScores): number {
  const weights = {
    hook: 0.25,          // 钩子（决定是否看下去）
    content: 0.25,       // 内容质量（决定是否看完）
    engagement: 0.2,     // 互动（决定是否推荐）
    conversion: 0.15,    // 转粉（决定账号价值）
    clickThrough: 0.15,  // 点击率（决定流量上限）
  };

  let total = 0;
  for (const [dim, weight] of Object.entries(weights)) {
    total += (dimensionScores[dim] ?? 60) * weight;
  }
  return Math.round(total);
}
```

### 6.4 行业基准库

按平台 × 粉丝量级 × 内容类型三维度划分基准：

```
benchmarks/
├── douyin/
│   ├── video_0-1k.json
│   ├── video_1k-10k.json
│   ├── video_10k-100k.json
│   ├── video_100k-1M.json
│   └── video_1M+.json
├── xiaohongshu/
│   ├── image-text_0-1k.json
│   ├── image-text_1k-10k.json
│   └── ...
└── kuaishou/
    └── ...
```

> 📐 **基准来源**：初期使用行业公开数据作为默认值，后续可根据用户匿名数据（需用户授权）动态校准，形成更精准的基准库。

### 6.5 LLM 增强（第二阶段）

规则引擎提供结构化的诊断骨架，LLM 负责将其转化为自然语言的、有温度的、个性化的诊断报告。

```
规则引擎输出（结构化）
    ↓
输入给 LLM：
  · 作品数据指标
  · 识别出的问题列表
  · 作品标题/描述
  · 用户账号定位
    ↓
LLM 输出：
  · 自然语言诊断总结（一段式）
  · 每条问题的个性化解读
  · 更具体的优化建议（结合账号定位）
  · 标题/封面改写建议
```

**选型建议**：
- 支持用户配置自己的 API Key（OpenAI / 豆包 / 通义千问 / 智谱等）
- 本地模型作为备选（如 Ollama + Qwen2）
- 不使用时默认纯规则引擎，不强制联网

---

## 7. 对标分析模块

### 7.1 两种对标模式

| 模式 | 数据来源 | 数据粒度 | 实现难度 | 优先级 |
|------|---------|---------|---------|--------|
| **模式 A：自有账号互标** | FlowX 中已授权的同平台多个账号 | 完整后台数据（完播率/画像/流量来源） | 低（复用采集逻辑） | **P0** |
| **模式 B：公开主页对标** | 竞品账号的公开主页 | 仅公开数据（点赞/评论/收藏/粉丝） | 中（需处理公开页结构） | P1 |

### 7.2 模式 A：自有账号互标

**使用场景**：用户在 FlowX 中授权了同一个平台的多个账号（如两个抖音号），可以直接选择其中一个或多个作为对标账号。

**优势**：
- 数据完整度 100%（所有后台指标都有）
- 采集成本为零（复用已有采集逻辑）
- 对比维度丰富，价值高

**对比维度**：

| 对比维度 | 说明 |
|---------|------|
| 粉丝量级对比 | 总粉丝数、增长速度、增长趋势 |
| 互动率对比 | 平均互动率、互动率趋势 |
| 完播率对比 | 平均完播率、完播率分布 |
| 发布频率对比 | 周/月发布数量、发布时间分布 |
| 爆款率对比 | 爆款（播放/点赞 Top 10%）占比 |
| 内容类型分布 | 不同内容类型的数量与表现 |
| 最佳发布时间 | 高互动作品的发布时段分析 |

### 7.3 模式 B：公开主页对标

**使用场景**：用户输入竞品账号的主页 URL 或 ID，系统采集其公开数据进行对比。

**技术实现**：
- 打开对标账号的公开主页
- 解析粉丝数、作品数、获赞数等公开数据
- 滚动加载最近 N 条作品的公开指标（点赞/评论/收藏）
- 保存为 BenchmarkSnapshot

**公开页采集挑战与对策**：

| 挑战 | 解决方案 |
|------|---------|
| 登录墙（未登录看不到数据） | 使用已登录账号的 partition 访问（复用 FlowX 中任一同平台账号的登录态） |
| 反爬/限流 | 降低采集频率、随机延时、模拟真人滚动 |
| 数据不完整（无非公开指标） | 在 UI 上明确标注"公开数据"，与自有账号数据区分 |
| 页面结构变化快 | 选择器定期维护，增加多套 fallback 选择器 |

### 7.4 对标对比 UI 设计

**图 3：对标分析页布局**

```
┌─────────────────────────────────────────────────────────────────────┐
│  📊 对标分析   [+ 添加对标账号]   [切换对比维度 ▾]  [导出对比报告]    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─ 选择对标账号 ────────────────────────────────────────────────┐  │
│  │  ☑️ 我的主账号 (douyin)      ☑️ 对标账号A    ☐ 对标账号B      │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌─ 核心指标对比（卡片式）───────────────────────────────────────┐  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐         │  │
│  │  │ 粉丝总数  │ │ 平均互动率│ │ 平均完播率│ │ 周发布数  │ ...    │  │
│  │  │ 主: 12.3k│ │ 主: 5.2% │ │ 主: 35%  │ │ 主: 3.5  │         │  │
│  │  │ 标A: 45k │ │ 标A: 8.1%│ │ 标A: 48% │ │ 标A: 5.2 │         │  │
│  │  │  ↑差距↑  │ │  ↓差距↓  │ │  ↓差距↓  │ │  ↓差距↓  │         │  │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘         │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌─ 粉丝增长趋势对比（折线图）───────────────────────────────────┐  │
│  │                                                                 │  │
│  │    ▲                                                           │  │
│  │ 粉│     ╭────── 对标账号A                                      │  │
│  │ 丝│    ╱                                                       │  │
│  │ 数│   ╱     ────── 我的账号                                    │  │
│  │   │  ╱  ╱                                                     │  │
│  │   │ ╱  ╱                                                      │  │
│  │   └──────────────────────────────→ 时间                        │  │
│  │                                                                 │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌─ 爆款作品对比 TOP10 ─────────────────────────────────────────┐  │
│  │  排名 │ 我的账号          │ 对标账号A          │ 差距分析    │  │
│  │   1   │ xxx作品 (12.3w赞) │ yyy作品 (45.6w赞) │ 3.7倍差距   │  │
│  │   2   │ ...               │ ...               │ ...         │  │
│  │  ...                                                                   │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌─ 内容策略对比 ────────────────────────────────────────────────┐  │
│  │  · 发布频率：主账号每周 3.5 条 vs 对标账号每周 5.2 条          │  │
│  │  · 平均时长：主账号 45s vs 对标账号 68s                        │  │
│  │  · 最佳发布时段：主账号 20:00 vs 对标账号 19:00                │  │
│  │  · 热门话题对比：...                                           │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 7.5 竞品动态监控

对标账号启用后，系统定期（可配置，默认每日）自动采集其最新动态：

- 新增作品检测（有新作品时记录）
- 高互动作品预警（点赞/评论超过阈值时通知）
- 粉丝量突变提醒（突然暴涨或暴跌）

通知方式：
- 应用内通知（Electron Notification API）
- 系统通知栏提醒
- 对标分析页红点/数字角标

---

## 8. 前端页面设计

### 8.1 页面结构与路由

```
src/renderer/pages/analytics/
├── Overview.vue          # 数据概览页（仪表盘）
├── Works.vue             # 作品列表页
├── WorkDetail.vue        # 作品详情 + 诊断报告
├── Benchmark.vue         # 对标分析页
└── components/
    ├── MetricCard.vue          # 指标卡片组件
    ├── TrendChart.vue          # 趋势图组件（ECharts 封装）
    ├── WorkListTable.vue       # 作品列表表格
    ├── DiagnosisPanel.vue      # 诊断面板组件
    ├── BenchmarkCompare.vue    # 对比组件
    └── HealthScoreGauge.vue    # 健康度仪表盘
```

**路由配置**：

| 路由 | 页面 | 说明 |
|------|------|------|
| `/analytics/overview` | 数据概览 | 账号核心指标 + 健康度 + 趋势 |
| `/analytics/works` | 作品列表 | 所有作品列表 + 筛选 + 排序 |
| `/analytics/work/:id` | 作品详情 | 单作品数据 + 诊断报告 + 优化建议 |
| `/analytics/benchmark` | 对标分析 | 对标账号管理 + 对比看板 |

左侧导航栏新增"数据分析"菜单，展开后包含以上子页面。

### 8.2 数据概览页（Overview）

**图 4：数据概览页布局**

```
┌─────────────────────────────────────────────────────────────────────┐
│  📊 数据概览                     [账号选择 ▾]  [刷新数据 ↻]         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─ 健康度评分 ───────────────┐  ┌─ 核心指标 ───────────────────┐ │
│  │                            │  │  ┌──────┐ ┌──────┐ ┌──────┐ │ │
│  │     ╭─────────────╮       │  │  │ 粉丝数│ │总点赞│ │作品数│ │ │
│  │    ╱   78 分       ╲      │  │  │ 12.3k │ │ 5.6w │ │  86  │ │ │
│  │   │   健康状态: 良好  │    │  │  └──────┘ └──────┘ └──────┘ │ │
│  │    ╲               ╱      │  │  ┌──────┐ ┌──────┐ ┌──────┐ │ │
│  │     ╰─────────────╯       │  │  │平均互动│ │完播率│ │涨粉数│ │ │
│  │                            │  │  │ 5.2%  │ │ 35%  │ │ +238 │ │ │
│  │  较上周：↑ +3 分           │  │  └──────┘ └──────┘ └──────┘ │ │
│  │  击败了 65% 的同量级账号    │  │                                │ │
│  └────────────────────────────┘  └────────────────────────────────┘ │
│                                                                     │
│  ┌─ 粉丝增长趋势（近30天）───────────────────────────────────────┐  │
│  │  📈 粉丝数                                    [7天/30天/90天] │  │
│  │                                                                 │  │
│  │   12.5k ┤            ╭──╮                                     │  │
│  │   12.0k ┤        ╭───╯  ╰╮                                    │  │
│  │   11.5k ┤    ╭───╯        ╰─                                  │  │
│  │   11.0k ┤╭───╯                                                │  │
│  │        ┬┴──┬──┬──┬──┬──┬──┬──┬──┬──┬──┬                       │  │
│  │          7/1  7/5  7/10  7/15  7/20  7/24                     │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌─ 最近作品表现 ────────────────────────────────────────────────┐  │
│  │  📋 最近发布的 5 条作品                   [查看全部 →]          │  │
│  │  ┌────┬──────────────┬──────┬──────┬──────┬──────┬────────┐   │  │
│  │  │封面│ 标题          │ 播放 │ 点赞 │ 评论 │完播率│ 诊断    │   │  │
│  │  ├────┼──────────────┼──────┼──────┼──────┼──────┼────────┤   │  │
│  │  │ 🖼 │ 这是第一条作品 │ 1.2w │  520 │  32  │ 42%  │ 良好 🟢│   │  │
│  │  │ 🖼 │ 第二条作品     │ 8.5k │  230 │  15  │ 28%  │ 待优化🟡│   │  │
│  │  │ 🖼 │ 第三条作品     │ 3.2k │   89 │   8  │ 15%  │ 问题 🔴│   │  │
│  │  └────┴──────────────┴──────┴──────┴──────┴──────┴────────┘   │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 8.3 作品列表页（Works）

- 支持按时间、播放量、点赞数、评论数、完播率、互动率排序
- 支持按内容类型、时间范围筛选
- 每条作品显示核心指标 + 诊断状态标签（良好/待优化/问题）
- 点击作品进入详情页

### 8.4 作品详情 + 诊断页（WorkDetail）

**图 5：作品诊断页布局**

```
┌─────────────────────────────────────────────────────────────────────┐
│  ← 返回作品列表          📝 作品标题（点击打开原视频）               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─ 作品信息 ────────────────────────────────────────────────────┐  │
│  │  🖼 封面图         标题：xxx                                    │  │
│  │                  发布时间：2026-07-20 18:30                    │  │
│  │                  时长：00:45                                   │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌─ 综合评分 ───────┐  ┌─ 各维度得分 ───────────────────────────┐  │
│  │                   │  │                                        │  │
│  │     ╭───╮         │  │  钩子质量      ████████░░ 82  良好     │  │
│  │    ╱ 72  ╲        │  │  内容质量      ██████░░░░ 65  待优化   │  │
│  │   │  良好  │       │  │  互动转化      ████████░░ 78  良好     │  │
│  │    ╲     ╱        │  │  转粉效率      █████░░░░░ 55  待优化   │  │
│  │     ╰───╯         │  │  封面点击率    █████████░ 90  优秀     │  │
│  │                   │  │                                        │  │
│  └───────────────────┘  └────────────────────────────────────────┘  │
│                                                                     │
│  ┌─ 核心数据指标 ────────────────────────────────────────────────┐  │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │  │
│  │  │ 播放量│ │ 点赞 │ │ 评论 │ │ 收藏 │ │ 转发 │ │ 涨粉 │    │  │
│  │  │ 12.3w│ │ 5,234│ │  328 │ │ 1,205│ │  856 │ │  +28 │    │  │
│  │  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘    │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │  │
│  │  │ 完播率   │ │ 平均时长  │ │ 曝光量   │ │ 点击率   │       │  │
│  │  │   35%    │ │   16s    │ │  25.6w   │ │  48%     │       │  │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌─ 🔍 问题诊断 ────────────────────────────────────────────────┐  │
│  │                                                                 │  │
│  │  ⚠️ 中风险 · 转粉效率偏低                                        │  │
│  │  转粉率仅 0.023%，低于同量级账号 0.05% 的基准线。                │  │
│  │  视频播放量不错，但观众看完就走，没有关注的理由。                  │  │
│  │                                                                 │  │
│  │  ⚠️ 中风险 · 内容完播率一般                                      │  │
│  │  完播率 35%，低于 40% 的行业基准。                               │  │
│  │  数据显示进度条在 50% 位置有明显下滑，中段内容可能拖沓。           │  │
│  │                                                                 │  │
│  │  💡 低风险 · 评论互动有提升空间                                  │  │
│  │  评论率 0.27%，处于中等水平，可以增加讨论话题。                  │  │
│  │                                                                 │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌─ 💡 优化建议 ────────────────────────────────────────────────┐  │
│  │                                                                 │  │
│  │  🎯 高优先级 · 强化关注引导                                      │  │
│  │  在视频结尾明确告诉观众"关注我，每天更新xxx"，                    │  │
│  │  让观众知道关注你的价值。                                        │  │
│  │  ✅ 可执行：在视频最后 3 秒加入口播 + 文字字幕"关注我 →"         │  │
│  │                                                                 │  │
│  │  🎯 高优先级 · 优化中段内容节奏                                  │  │
│  │  视频 20-30 秒区间观众流失明显，建议精简这部分内容，               │  │
│  │  或加入一个转折/反转来重新吸引注意力。                            │  │
│  │  ✅ 可执行：回看 20-30s 片段，删掉不重要的细节，加快节奏          │  │
│  │                                                                 │  │
│  │  💡 中优先级 · 增加评论区引导                                    │  │
│  │  在内容中设置一个开放性问题，引导观众在评论区讨论。                │  │
│  │                                                                 │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  [重新诊断]  [用 AI 深度分析]  [导出诊断报告]                      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 8.5 图表库选型

推荐使用 **ECharts**，理由：

| 维度 | 说明 |
|------|------|
| **功能丰富** | 折线图、柱状图、饼图、仪表盘、雷达图全覆盖，满足数据可视化需求 |
| **中文文档好** | 官方文档完善，社区活跃，问题容易搜索到 |
| **性能好** | Canvas 渲染，大数据量也流畅 |
| **Vue 集成** | `echarts-for-vue` 或自行封装，简单易用 |
| **扩展性** | 支持自定义主题、动画、交互 |

封装一个通用的 `TrendChart.vue` 组件，统一配置和样式，各页面复用。

---

## 9. IPC 通道设计

### 9.1 通道总览

新增 `src/main/ipc/analytics.ts`，使用与现有 IPC 一致的 `safeInvoke` 模式。

```typescript
// src/main/ipc/analytics.ts

// 数据采集相关
safeInvoke('analytics:collectOverview', collectOverview);      // 采集账号概览
safeInvoke('analytics:collectWorks', collectWorks);            // 采集作品列表
safeInvoke('analytics:collectWorkDetail', collectWorkDetail);  // 采集单作品详情
safeInvoke('analytics:collectAll', collectAll);                // 全量采集（概览+作品）
safeInvoke('analytics:getCollectStatus', getCollectStatus);    // 获取采集任务状态

// 数据查询相关
safeInvoke('analytics:getAccountStats', getAccountStats);      // 获取账号统计（快照列表）
safeInvoke('analytics:getWorks', getWorks);                    // 获取作品列表（分页+筛选）
safeInvoke('analytics:getWorkDetail', getWorkDetail);          // 获取作品详情+指标历史
safeInvoke('analytics:getWorkMetricsHistory', getMetricsHistory); // 作品指标历史曲线

// 诊断相关
safeInvoke('analytics:diagnoseWork', diagnoseWork);            // 诊断单作品
safeInvoke('analytics:diagnoseAll', diagnoseAll);              // 批量诊断所有作品
safeInvoke('analytics:getDiagnosis', getDiagnosis);            // 获取已缓存的诊断结果

// 对标账号相关
safeInvoke('analytics:addBenchmark', addBenchmark);            // 添加对标账号
safeInvoke('analytics:updateBenchmark', updateBenchmark);      // 更新对标账号
safeInvoke('analytics:deleteBenchmark', deleteBenchmark);      // 删除对标账号
safeInvoke('analytics:getBenchmarks', getBenchmarks);          // 获取对标账号列表
safeInvoke('analytics:collectBenchmark', collectBenchmark);    // 采集对标账号数据
safeInvoke('analytics:compareWithBenchmark', compareWithBenchmark); // 对比分析

// 配置相关
safeInvoke('analytics:getConfig', getConfig);                  // 获取分析配置
safeInvoke('analytics:updateConfig', updateConfig);            // 更新分析配置

// 事件推送（主进程 → 渲染层）
// analytics:collectProgress    — 采集进度更新
// analytics:collectComplete    — 采集完成通知
// analytics:benchmarkUpdate    — 对标数据更新通知
```

### 9.2 关键通道详细设计

#### analytics:getWorks — 获取作品列表（分页+筛选）

**查询参数（WorksQueryParams）**：

```typescript
interface WorksQueryParams {
  /** 系统内部账号 ID（单个） */
  accountId?: string;
  /** 系统内部账号 ID 列表（多选） */
  accountIds?: string[];
  /** 平台账号 ID 列表（多选，平台原生标识，如抖音号/小红书号） */
  platformAccountIds?: string[];
  /** 平台类型 */
  platform?: PlatformType;
  /** 内容类型 */
  contentType?: ContentType;
  /** 关键词搜索（匹配标题） */
  keyword?: string;
  /** 开始时间戳（毫秒） */
  startTime?: number;
  /** 结束时间戳（毫秒） */
  endTime?: number;
  /** 排序字段（publishTime / views / likes / comments / favorites / shares） */
  sortBy?: string;
  /** 排序方向（asc / desc） */
  sortOrder?: 'asc' | 'desc';
  /** 页码（从 1 开始） */
  page?: number;
  /** 每页数量 */
  pageSize?: number;
}
```

**返回值（PagedResult）**：

```typescript
interface PagedResult<T> {
  /** 当前页数据 */
  items: T[];
  /** 总条数 */
  total: number;
  /** 当前页码 */
  page: number;
  /** 每页条数 */
  pageSize: number;
  /** 总页数 */
  totalPages: number;
}
```

**筛选优先级说明**：
- `accountId` / `accountIds` / `platformAccountIds` 可以同时使用，取交集
- 推荐使用 `platformAccountIds` 进行账号筛选，因为平台原生标识更稳定，跨系统一致
- 所有筛选条件均为 AND 关系

#### analytics:collectAll — 全量采集

**入参**：
```typescript
interface CollectAllParams {
  accountId: string;
  /** 是否强制全量采集（false = 增量模式） */
  forceFull?: boolean;
  /** 作品数量上限 */
  workLimit?: number;
}
```

**返回**：
```typescript
interface CollectAllResult {
  taskId: string;
  status: 'running' | 'success' | 'failed';
  /** 已采集作品数 */
  collectedCount: number;
  /** 总作品数（预估） */
  totalCount?: number;
}
```

**进度事件**（`analytics:collectProgress`）：
```typescript
interface CollectProgressEvent {
  taskId: string;
  phase: 'overview' | 'works' | 'detail';
  current: number;
  total: number;
  message?: string;
}
```

#### analytics:diagnoseWork — 作品诊断

**入参**：
```typescript
interface DiagnoseWorkParams {
  accountId: string;
  workId: string;
  /** 是否使用 LLM 增强（需要用户配置） */
  useLLM?: boolean;
}
```

**返回**：
```typescript
WorkDiagnosis  // 见 §3.4
```

#### analytics:compareWithBenchmark — 对标对比

**入参**：
```typescript
interface CompareParams {
  ownerAccountId: string;
  benchmarkIds: string[];
  /** 对比维度 */
  dimensions?: string[];
  /** 时间范围 */
  timeRange?: '7d' | '30d' | '90d' | 'all';
}
```

**返回**：
```typescript
interface CompareResult {
  accounts: Array<{
    id: string;
    name: string;
    avatar?: string;
    isOwner: boolean;
  }>;
  metrics: Array<{
    key: string;
    name: string;
    values: Record<string, number | string>; // accountId → 数值
    unit?: string;
  }>;
  trendData: Array<{
    date: string;
    values: Record<string, number>;
  }>;
  topWorks: Array<{
    rank: number;
    works: Record<string, { title: string; value: number; metric: string }>;
  }>;
}
```

### 9.3 Preload 暴露

在 `src/preload/index.ts` 的 `electronApi` 中新增 `analytics` 命名空间：

```typescript
// preload/index.ts 扩展
analytics: {
  collectOverview: (accountId: string) => invoke('analytics:collectOverview', accountId),
  collectWorks: (accountId: string, options?) => invoke('analytics:collectWorks', accountId, options),
  collectAll: (params) => invoke('analytics:collectAll', params),
  getCollectStatus: (taskId) => invoke('analytics:getCollectStatus', taskId),
  getAccountStats: (accountId, options?) => invoke('analytics:getAccountStats', accountId, options),
  getWorks: (accountId, options?) => invoke('analytics:getWorks', accountId, options),
  getWorkDetail: (accountId, workId) => invoke('analytics:getWorkDetail', accountId, workId),
  diagnoseWork: (accountId, workId, options?) => invoke('analytics:diagnoseWork', accountId, workId, options),
  addBenchmark: (data) => invoke('analytics:addBenchmark', data),
  updateBenchmark: (id, data) => invoke('analytics:updateBenchmark', id, data),
  deleteBenchmark: (id) => invoke('analytics:deleteBenchmark', id),
  getBenchmarks: (ownerAccountId?) => invoke('analytics:getBenchmarks', ownerAccountId),
  collectBenchmark: (benchmarkId) => invoke('analytics:collectBenchmark', benchmarkId),
  compareWithBenchmark: (params) => invoke('analytics:compareWithBenchmark', params),
  getConfig: () => invoke('analytics:getConfig'),
  updateConfig: (config) => invoke('analytics:updateConfig', config),
  onCollectProgress: (callback) => on('analytics:collectProgress', callback),
  onCollectComplete: (callback) => on('analytics:collectComplete', callback),
}
```

---

## 10. 存储方案

### 10.1 MVP 阶段：electron-store

初期数据量不大时，继续使用项目已有的 `electron-store` + `SecureStore` 体系。

```typescript
// SecureStore Schema 新增字段
interface StoreSchema {
  // ... 现有字段 ...

  /** 账号分析数据 */
  analytics: {
    /** 作品数据（按 accountId 分组） */
    works: Record<string, WorkItem[]>;
    /** 作品指标快照（按 workId 分组，只存最新一条） */
    workMetrics: Record<string, WorkMetrics>;
    /** 账号统计快照（按 accountId 分组，存最近 90 天） */
    accountStats: Record<string, AccountStatsSnapshot[]>;
    /** 诊断结果缓存（按 workId 分组） */
    diagnoses: Record<string, WorkDiagnosis>;
    /** 对标账号配置 */
    benchmarks: BenchmarkAccount[];
    /** 对标数据快照 */
    benchmarkSnapshots: Record<string, BenchmarkSnapshot[]>;
    /** 分析配置 */
    config: AnalyticsConfig;
  };
}
```

**容量评估**：

| 数据类型 | 单条大小 | 100 条作品 | 1000 条作品 | 说明 |
|---------|---------|-----------|------------|------|
| WorkItem | ~0.2KB | 20KB | 200KB | 作品基础信息 |
| WorkMetrics | ~0.3KB | 30KB | 300KB | 最新一条快照 |
| 账号统计快照 | ~0.2KB/天 | 6KB（30天） | 6KB | 每天一条 |
| 诊断结果 | ~1KB | 100KB | 1MB | 每条作品一条 |
| 对标账号 | ~0.3KB | - | - | 一般 5-10 个 |
| **合计** | - | **~160KB** | **~1.5MB** | 完全可控 |

### 10.2 升级阶段：better-sqlite3

当数据量上来（作品数 > 500 或需要更长时间序列），迁移到 `better-sqlite3`。

**优势**：
- 关系型数据库，适合时间序列数据查询和聚合
- 支持复杂查询（排序、筛选、分组、JOIN）
- 事务支持，数据一致性更好
- 单文件数据库，部署简单

**表设计**：

```sql
-- 作品表
CREATE TABLE works (
  id TEXT PRIMARY KEY,
  platform TEXT NOT NULL,
  account_id TEXT NOT NULL,
  title TEXT NOT NULL,
  cover_url TEXT,
  content_type TEXT NOT NULL,
  publish_time INTEGER NOT NULL,
  detail_url TEXT,
  duration INTEGER,
  first_collected_at INTEGER NOT NULL,
  last_updated_at INTEGER NOT NULL,
  UNIQUE(platform, account_id, id)
);

-- 作品指标快照表（时间序列）
CREATE TABLE work_metrics (
  id TEXT PRIMARY KEY,
  work_id TEXT NOT NULL,
  account_id TEXT NOT NULL,
  platform TEXT NOT NULL,
  views INTEGER NOT NULL DEFAULT 0,
  likes INTEGER NOT NULL DEFAULT 0,
  comments INTEGER NOT NULL DEFAULT 0,
  favorites INTEGER NOT NULL DEFAULT 0,
  shares INTEGER NOT NULL DEFAULT 0,
  completion_rate REAL,
  avg_play_duration REAL,
  new_fans INTEGER,
  impressions INTEGER,
  click_rate REAL,
  interaction_rate REAL,
  fan_conversion_rate REAL,
  collected_at INTEGER NOT NULL,
  FOREIGN KEY (work_id) REFERENCES works(id),
  INDEX idx_work_collected (work_id, collected_at),
  INDEX idx_account_collected (account_id, collected_at)
);

-- 账号每日统计表
CREATE TABLE account_stats (
  id TEXT PRIMARY KEY,
  account_id TEXT NOT NULL,
  date TEXT NOT NULL,
  fans_count INTEGER NOT NULL,
  follow_count INTEGER,
  total_like_count INTEGER,
  new_fans INTEGER,
  total_views INTEGER,
  total_interactions INTEGER,
  works_published INTEGER,
  avg_completion_rate REAL,
  avg_interaction_rate REAL,
  collected_at INTEGER NOT NULL,
  UNIQUE(account_id, date)
);

-- 诊断结果表
CREATE TABLE work_diagnoses (
  work_id TEXT PRIMARY KEY,
  overall_score INTEGER NOT NULL,
  dimension_scores TEXT NOT NULL,  -- JSON
  issues TEXT NOT NULL,            -- JSON
  suggestions TEXT NOT NULL,       -- JSON
  generated_at INTEGER NOT NULL,
  engine_version TEXT NOT NULL,
  FOREIGN KEY (work_id) REFERENCES works(id)
);

-- 对标账号表
CREATE TABLE benchmark_accounts (
  id TEXT PRIMARY KEY,
  owner_account_id TEXT NOT NULL,
  platform TEXT NOT NULL,
  profile_url TEXT NOT NULL,
  name TEXT NOT NULL,
  avatar TEXT,
  platform_user_id TEXT,
  type TEXT NOT NULL,
  remark TEXT,
  last_collected_at INTEGER,
  created_at INTEGER NOT NULL,
  enabled INTEGER NOT NULL DEFAULT 1
);
```

**迁移策略**：
- 提供一键迁移工具（electron-store → SQLite）
- 检测到数据库不存在时自动创建
- 旧数据保留备份，用户可随时回滚

---

## 11. 技术坑点与解决方案

### 11.1 平台数据页面结构不稳定

**问题**：创作者后台的数据中心页面可能随平台版本更新而改变 DOM 结构，导致选择器失效。

**解决方案**：
1. **多层 fallback 选择器**：每个数据字段准备 2-3 套选择器（按优先级排序），第一套失效自动尝试下一套
2. **模糊匹配 + 关键词定位**：不依赖精确 class 名，而是通过"找到含'完播率'文字的元素 → 找相邻的数值元素"这种方式定位
3. **采集结果校验**：采集后校验数据合理性（如数值是否为正数、是否在合理范围），异常时告警
4. **选择器配置化**：平台选择器抽成独立配置文件，更新选择器不需要改核心逻辑

### 11.2 数据加载慢（SPA + 异步接口）

**问题**：创作者数据中心通常是 SPA，数据通过接口异步加载，DOM 渲染有延迟。

**解决方案**：
1. **等待策略**：导航后等待特定 DOM 元素出现（而不是固定 sleep），超时则重试
2. **轮询检测**：用 `setInterval` 轮询目标元素是否出现，出现后再执行提取
3. **监听网络请求**：通过 CDP 监听 `Network.responseReceived`，直接拦截数据接口响应（更高效、更稳定）
4. **兜底：固定等待 + 重试**：以上都失败时，固定等待 3-5 秒后尝试提取

### 11.3 登录态过期

**问题**：采集时账号登录态可能已过期，导致跳转到登录页。

**解决方案**：
1. **采集前检测**：先打开一个简单页面（如首页），检测登录态
2. **过期自动刷新**：检测到过期时，自动走 AccountService 的刷新流程
3. **失败明确告知**：采集失败后明确告诉用户"登录态已过期，请先刷新账号"，并提供快速刷新按钮

### 11.4 频繁采集触发风控

**问题**：过于频繁地打开数据中心页面，可能被平台判定为异常行为。

**解决方案**：
1. **采集频率限制**：最小采集间隔 2 小时（可配置，但不允许低于 30 分钟）
2. **随机延迟**：每次操作间加入随机延迟（500-2000ms），模拟真人操作节奏
3. **窗口复用**：尽量复用已有窗口，减少"打开-关闭"的频次
4. **用户可控**：所有采集都是用户主动触发或授权的自动采集，不在后台偷跑

### 11.5 浏览器窗口资源占用

**问题**：采集需要打开浏览器窗口，太多并行采集会占用大量内存和 CPU。

**解决方案**：
1. **队列控制**：同时最多 2 个采集任务（可配置，默认 2）
2. **窗口池**：采集完成后窗口保持 5 分钟，空闲后自动关闭
3. **隐藏窗口**：采集窗口默认 `show: false`，后台运行
4. **增量采集**：只采集新数据，不全量重复抓取

### 11.6 Electron WebContentsView 遮挡问题

**问题**：对标分析页如果内嵌浏览器视图，会有 z-index 遮挡问题（与规则编辑器同样的问题）。

**解决方案**：
1. **对标数据不实时展示网页**：采集在后台隐藏窗口进行，前端只展示采集后的数据
2. **对标账号主页查看**：用户点击"查看主页"时，在浏览器页（Browser.vue）打开新标签页
3. **避免 WebContentsView + 弹窗组合**：所有分析界面都是纯前端渲染，不叠加在浏览器视图上

---

## 12. 实施计划

### 12.1 已实现进度（v0.1.5）

| 模块 | 状态 | 说明 |
|------|------|------|
| **数据模型** | ✅ 已完成 | WorkItem / WorkMetrics / WorksQueryParams 完整类型定义，含 platformAccountId 字段 |
| **存储层** | ✅ 已完成 | electron-store 封装，key = `analyticsData`，按 accountId 分桶存储 |
| **Collector 体系** | ✅ 已完成 | BaseCollector 基类 + 4个平台 Collector（抖音/小红书/快手/微信视频号） |
| **作品列表采集** | ✅ 已完成 | 支持抖音、小红书、快手、微信视频号（视频+图文）的作品列表采集 |
| **平台账号ID采集** | ✅ 已完成 | 采集时自动保存各平台原生账号标识（抖音号/小红书号/快手号/视频号等） |
| **真分页** | ✅ 已完成 | 服务端分页，默认 20 条/页，支持 page / pageSize 参数 |
| **多条件筛选** | ✅ 已完成 | 平台筛选、账号筛选（使用平台账号ID）、关键词搜索、多维度排序 |
| **搜索按钮** | ✅ 已完成 | 筛选条件变更不立即查询，点击"搜索"按钮后执行，支持一键重置 |
| **前端页面** | ✅ 已完成 | 账号分析页（AnalyticsPanel.vue），作品列表 + 筛选 + 采集控制 |
| **UI 优化** | ✅ 已完成 | 平台图标（本地资源）、账号下拉框头像+昵称+平台账号ID、列表账号列优化 |
| **微信视频号图文** | ✅ 已完成 | 支持视频号图文作品采集，脚本重构避免模板字符串转义问题 |
| **并发控制** | ✅ 已完成 | 采集任务队列，默认 2 并发，支持进度通知 |

### 12.2 里程碑规划

| 阶段 | 版本 | 内容 | 开发量 | 优先级 |
|------|------|------|--------|--------|
| **MVP 基础版** | v0.5.0 | 数据采集 + 作品列表 + 基础指标展示 | 中 | P0 |
| **诊断增强版** | v0.6.0 | 规则引擎 + 作品诊断 + 优化建议 | 中高 | P0 |
| **对标分析版** | v0.7.0 | 自有账号互标 + 对比看板 | 中 | P1 |
| **趋势洞察版** | v0.8.0 | 账号健康度 + 趋势图 + 数据概览完善 | 中 | P1 |
| **LLM 增强版** | v0.9.0 | AI 深度分析 + 自然语言报告 + 个性化建议 | 中 | P2 |
| **公开对标版** | v1.0.0 | 公开页对标 + 竞品监控 + 预警通知 | 中高 | P2 |
| **视觉分析版** | v1.1.0 | CDP 封面质量评估 + 高光时刻识别 | 高 | P3 |

### 12.3 MVP 阶段任务完成情况

#### 任务 1：数据模型 + 存储层

- [x] 定义核心类型（WorkItem / WorkMetrics / WorksQueryParams 等）到 `src/types/index.ts`
- [x] 创建 `AnalyticsStore`（electron-store 封装）
- [x] 实现基础 CRUD 方法
- [x] 支持多账号数据聚合查询
- [x] 支持 platformAccountId 字段

#### 任务 2：Collector 体系骨架

- [x] 创建 `BaseCollector` 基类（窗口管理 + cookie 注入 + evalJS + 导航等待）
- [x] 创建采集任务队列（并发控制 + 进度通知）
- [x] 窗口池实现（复用 + 空闲自动关闭）

#### 任务 3：各平台 Collector 实现

- [x] 抖音：作品列表采集（标题/封面/发布时间/点赞/评论/收藏/分享）
- [x] 小红书：作品列表采集（CDP 监听网络请求 + DOM 提取双策略）
- [x] 快手：作品列表采集
- [x] 微信视频号：视频作品采集
- [x] 微信视频号：图文作品采集（独立列表，脚本重构）
- [x] 各平台 platformAccountId 提取与保存

#### 任务 4：AnalyticsService 总调度

- [x] 封装采集流程（创建窗口 → 注入 cookie → 导航 → 采集 → 保存 → 关闭窗口）
- [x] IPC 通道注册（数据采集 + 查询）
- [x] Preload 暴露 API
- [x] 真分页实现（服务端分页 + 多条件过滤 + 排序）

#### 任务 5：前端基础页面

- [x] 新增账号分析页面（AnalyticsPanel.vue） + 左侧导航菜单
- [x] `analyticsStore` (Pinia) 状态管理
- [x] 作品列表页：表格 + 排序 + 筛选 + 分页
- [x] 平台筛选（下拉框，带平台图标）
- [x] 账号筛选（多选下拉框，头像+昵称+平台账号ID）
- [x] 关键词搜索
- [x] 搜索/重置按钮（筛选条件变更不立即查询）
- [x] 列表账号列显示平台图标+平台名称+平台账号ID

#### 任务 6：测试 + 打磨

- [x] 各平台采集功能测试
- [x] 筛选功能测试（平台/账号/关键词）
- [x] 真分页验证
- [x] UI/UX 优化（平台图标、头像错位修复）
- [x] 微信视频号图文采集修复（模板字符串转义问题）

### 12.4 后续阶段排期建议

| 版本 | 建议周期 | 核心交付 |
|------|---------|---------|
| v0.5 MVP | 2-3 周 | 抖音数据能采进来、能展示 |
| v0.6 诊断 | 1-2 周 | 作品诊断报告能生成、有价值 |
| v0.7 对标 | 1 周 | 自有多个账号能互相对比 |
| v0.8 趋势 | 1 周 | 数据概览仪表盘 + 趋势图 |
| v0.9 LLM | 1 周 | AI 深度分析接入 |
| v1.0 公开对标 | 2 周 | 竞品监控 + 预警 |
| v1.1 视觉分析 | 3 周+ | CDP 视觉能力（护城河功能） |

### 12.5 风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 某平台数据页面结构复杂，采集难度大 | 中 | 高 | 先做抖音（相对规范），其他平台后续补；实在采不到的指标标注"暂不支持" |
| 平台频繁改版，选择器经常失效 | 中 | 中 | 选择器配置化 + 多层 fallback + 快速更新机制 |
| 采集触发平台风控 | 低 | 高 | 严格控制频率 + 随机延迟 + 用户主动触发为主 |
| 用户对"诊断建议"期望值过高 | 中 | 中 | 明确标注"AI 建议仅供参考"，规则引擎先上，LLM 标注为实验功能 |
| 数据量超预期，electron-store 不够用 | 低 | 中 | 提前设计好存储抽象层，后续迁移 SQLite 成本可控 |

---

> ✅ **当前状态**：MVP 基础版已完成（v0.1.5），支持抖音/小红书/快手/微信视频号的作品数据采集、多条件筛选、真分页、平台账号ID筛选。
>
> 📌 **下一步行动**：
> 1. 完善账号概览数据采集（粉丝/获赞/作品数等）
> 2. 实现作品诊断引擎（规则引擎 + 行业基准）
> 3. 添加数据趋势图（ECharts 集成）
> 4. 对标分析功能（自有账号互标）

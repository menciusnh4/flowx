# 抖音文章（长文）发布技术文档

> 发布入口：`https://creator.douyin.com/creator-micro/content/upload?default-tab=5`

## 一、文章发布 vs 图文发布 vs 视频发布

| 维度 | 视频发布 | 图文发布 | 文章发布 |
|------|---------|---------|---------|
| 入口 URL | 默认上传页 | `?default-tab=3` | `?default-tab=5` |
| 标题限制 | 55 字 | 80 字 | **30 字** |
| 正文限制 | —（标题即可发布） | 1000 字 | **8000 字** |
| 文章摘要 | 无 | 无 | **30 字限制，独立输入框** |
| 素材文件 | 视频（必填） | 图片（必填） | **封面图（必填）** |
| 话题标签 | 最多 10 个，正文末尾追加 | 最多 10 个，正文末尾追加 | **最多 5 个，通过专用弹窗添加** |
| 发布按钮 | `button.button-dhlUZE.primary-button` | `button.button-dhlUZE.primary-button` | `button.button-dhlUZE.primary-button` |

## 二、核心流程

```
┌─────────────────────────────────────────────────────────────────┐
│  1) loadURL(https://creator.douyin.com/creator-micro/          │
│          content/upload?default-tab=5)                         │
│     ↓                                                          │
│  2) 等待页面加载稳定（waitForStable）                           │
│     ↓                                                          │
│  3) 检测并点击"我要发文"引导页按钮（首次进入可能有引导页）       │
│     ↓                                                          │
│  4) 填写标题（30 字限制）                                       │
│     buildFillTitle(title, ARTICLE_TITLE_MAX=30)               │
│     - 抖音标题使用 React 受控组件（contenteditable）           │
│     - 🔑 使用 execCommand('insertText') 触发 onChange           │
│     - 超过 30 字自动截断                                        │
│     ↓                                                          │
│  5) 填写正文（8000 字限制，富文本编辑器）                        │
│     buildFillContent(content, ARTICLE_CONTENT_MAX=8000)       │
│     - 正文编辑器检测：article-content / editor / editor-text  │
│     - 自动分段写入（每 1000 字左右自然换行）                    │
│     - 超过 8000 字自动截断                                      │
│     ↓                                                          │
│  6) 填写话题（可选，最多 10 个）                                │
│     appendTagsToContent(tags)                                  │
│     ↓                                                          │
│  7) 等待保存草稿（抖音自动保存草稿，正文较多时需等待）           │
│     sleep(2000ms)                                              │
│     ↓                                                          │
│  8) 上传封面图（**核心难点**）                                  │
│     uploadArticleCover(win, coverFile, log)                    │
│     - 详见第三节「封面上传技术方案」                             │
│     ↓                                                          │
│  9) 填写文章摘要（30 字限制）                                   │
│     fillArticleSummary(win, summary, log)                     │
│     - 独立的摘要输入框（contenteditable）                      │
│     - execCommand('insertText') 模拟原生输入                    │
│     ↓                                                          │
│  10) 添加话题标签（可选，最多 5 个）                             │
│     addArticleTopicsViaPopup(win, tags, log)                  │
│     - 🔑 通过专用弹窗添加（非正文追加方式）                      │
│     - 点击"添加话题" → 弹出搜索弹窗 → 输入关键词 → 点击推荐项   │
│     - 详见第四节「话题弹窗添加技术方案」                         │
│     ↓                                                          │
│  11) 点击"发布"按钮                                              │
│     优先级:                                                      │
│       1) Class 选择器: button.button-dhlUZE.primary-button     │
│       2) 文本匹配: "发布" / "发布作品" / "发布文章"             │
│       3) 降级为 CDP 穿透 shadow DOM 点击                        │
│     ↓                                                          │
│ 12) 等待发布结果（60 秒超时）                                    │
│     检测 URL: 离开 upload 页 或 出现 "发布成功/已发布" 文本      │
│     ↓                                                          │
│ 13) 完成                                                       │
└─────────────────────────────────────────────────────────────────┘
```

## 三、封面上传技术方案（核心难点）

### 3.1 问题背景

抖音文章发布**必须上传封面图**，否则无法发布。封面上传区域的 DOM 结构：

```html
<div class="mycard-c48v6G">
  <div class="addIcon-WtgoEN"><span class="addInnerIcon-KEMTi2">+</span></div>
  <span class="mycard-info-uBuPL9">点击上传封面图</span>
  <span class="mycard-info-text-YmWBDj">
    <span class="mycard-info-text-span-qfhmNK">选择封面</span>
  </span>
</div>
```

**关键问题**：
- 页面中**不存在持久化的** `<input type="file">` 元素
- 点击时动态创建 input 或使用 `showOpenFilePicker()` API
- 传统的 `DOM.setFileInputFiles` 方案找不到 input 节点
- 文件选择对话框是浏览器原生弹窗，CDP 无法直接操作

### 3.2 解决方案：JS 层 DataTransfer 直接注入 + CDP 兜底

采用**四层拦截机制**，完全在 JavaScript 层面设置文件，不弹出文件选择对话框：

```
┌─────────────────────────────────────────────────────────────────┐
│                     封面上传五层防护架构                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Layer 1: CDP Page.setInterceptFileChooserDialog         │   │
│  │ 浏览器层面拦截文件选择对话框，确保对话框不会弹出          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Layer 2: pointerdown/mousedown/click 捕获阶段拦截        │   │
│  │ 在事件捕获阶段调用 preventDefault() 阻止默认行为          │   │
│  │ 同时查找附近的 file input，立即通过 DataTransfer 设置文件  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Layer 3: HTMLInputElement.prototype.click() 拦截        │   │
│  │ 重写 input.click() 方法，代码调用时直接设置文件            │   │
│  │ 不调用原始方法，不弹出对话框                               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Layer 4: MutationObserver 监听 DOM 变化                  │   │
│  │ 监听 document 变化，新出现的 file input 立即设置文件       │   │
│  │ 穿透 Shadow DOM 进行扫描                                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Layer 5: showOpenFilePicker() 拦截 (File System Access) │   │
│  │ 重写 showOpenFilePicker，直接返回伪造的 FileHandle        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ CDP 兜底：3秒后 JS 方案未成功则降级为 DOM.getDocument    │   │
│  │ pierce:true 穿透所有 iframe/shadow DOM 查找 input         │   │
│  │ 使用 DOM.setFileInputFiles 设置文件                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 核心代码：buildSuperCoverPatchScript

**文件设置核心**：使用 `DataTransfer` API 构造 FileList，通过 `Object.defineProperty` 注入到 input：

```javascript
function setFileToInput(input) {
  // 1. 使用 DataTransfer 创建 FileList
  var dt = new DataTransfer();
  dt.items.add(coverFile);  // coverFile 是从 base64 构造的 File 对象
  var files = dt.files;
  
  // 2. 重定义 input.files 属性（三种方法兜底）
  try {
    Object.defineProperty(input, 'files', {
      value: files,
      writable: true,
      configurable: true,
    });
  } catch (e) {
    // 尝试原型链 setter
    var desc = Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'files');
    if (desc && desc.set) desc.set.call(input, files);
  }
  
  // 3. 设置 value 属性
  Object.defineProperty(input, 'value', {
    value: 'C:\\fakepath\\' + coverFile.name,
    writable: true,
    configurable: true,
  });
  
  // 4. 按正确顺序触发事件（先 input 后 change，适配 React 合成事件）
  input.dispatchEvent(new Event('input', { bubbles: true }));
  input.dispatchEvent(new Event('change', { bubbles: true }));
}
```

### 3.4 base64 文件注入方案

封面图通过 Node.js 读取为 base64，注入到页面后在浏览器端构造 File 对象：

```javascript
// Node.js 侧
const fileBuffer = fs.readFileSync(coverFile);
const base64Data = fileBuffer.toString('base64');
const fileName = path.basename(coverFile);

// 注入到浏览器侧的 patch 脚本中
function base64ToFile(base64, filename, type) {
  var binaryStr = atob(base64);
  var bytes = new Uint8Array(binaryStr.length);
  for (var i = 0; i < binaryStr.length; i++) {
    bytes[i] = binaryStr.charCodeAt(i);
  }
  return new File([bytes], filename, { type: type });
}
```

### 3.5 跨 Frame 注入

patch 不仅注入主文档，还通过 `Page.createIsolatedWorld` 注入到所有 iframe：

```typescript
// 1. 获取 FrameTree
const frameTree = await win.webContents.debugger.sendCommand('Page.getFrameTree');

// 2. 遍历所有子 frame，创建隔离世界注入 patch
for (const childFrame of childFrames) {
  const isoRes = await win.webContents.debugger.sendCommand('Page.createIsolatedWorld', {
    frameId: childFrame.id,
    worldName: 'flowx-cover-' + Date.now(),
    grantUniversalAccess: true,
  });
  
  await win.webContents.debugger.sendCommand('Runtime.evaluate', {
    expression: superPatchScript,
    contextId: isoRes.executionContextId,
    returnByValue: true,
  });
}

// 3. 监听 Runtime.executionContextCreated，新 frame/context 创建时立即注入
```

### 3.6 弹窗关闭与"完成"按钮处理

封面上传后可能出现裁剪弹窗，需要点击"完成"按钮：

1. **点击前先发送 Escape 键**：关闭可能残留的文件选择对话框
2. **轮询点击"完成"按钮**：等待裁剪弹窗出现，重试10次（每次800ms）
3. **智能检测无弹窗情况**：如果检查到"编辑封面"文本或封面已有背景图，说明无裁剪弹窗，直接判定完成
4. **多次 Escape 兜底**：流程结束前再按3次 Escape 确保所有弹窗关闭

### 3.7 封面上传成功验证

三重验证机制：

```javascript
// 1. 检查封面区域是否有背景图
var coverImg = document.querySelector('.cover-Uudq5y');
if (coverImg) {
  var style = coverImg.getAttribute('style') || '';
  var hasBg = style.indexOf('background-image') !== -1 && style.indexOf('url(') !== -1;
  if (hasBg) return { hasCover: true };
}

// 2. 检查是否有"编辑封面"文本（上传成功后文本变化）
var allSpans = document.querySelectorAll('span');
for (var i = 0; i < allSpans.length; i++) {
  if ((allSpans[i].innerText || '').trim() === '编辑封面') {
    return { hasCover: true };
  }
}

// 3. 检查 mycard 文本变化（从"点击上传封面图"变为其他内容）
var mycard = document.querySelector('.mycard-c48v6G');
if (mycard) {
  var mycardText = (mycard.innerText || '').trim();
  if (mycardText.indexOf('点击上传封面图') === -1 && mycardText.length > 0) {
    return { hasCover: true };
  }
}
```

## 四、文章摘要技术方案

### 4.1 摘要输入框结构

抖音文章发布有独立的**文章摘要**输入框，30 字限制：

```html
<!-- 摘要输入框位于封面设置区域附近 -->
<div class="content-obt4oA new-layout-sLYOT6">
  <div>
    <div class="title-dS7kae">
      <span class="title-content-oaqcSp">文章摘要</span>
    </div>
  </div>
  <div class="content-child-V0CB7w content-limit-width-zybqBW">
    <!-- contenteditable 输入框 -->
    <div contenteditable="true" role="textbox">
      <!-- 用户输入的摘要内容 -->
    </div>
  </div>
</div>
```

### 4.2 填写方式

使用 `buildFillSummaryScript` 生成 JS 脚本，通过 `execCommand('insertText')` 模拟原生输入：

```typescript
async function fillArticleSummary(win: BrowserWindow, summary: string, log: any): Promise<boolean> {
  const script = buildFillSummaryScript(summary);
  const res = await evalJS(win, log, '文章-填写摘要', script);
  return res?.ok === true;
}
```

**关键要点**：
- 摘要 30 字硬限制，超过自动截断
- 使用 `execCommand('insertText')` 确保 React 受控组件同步更新
- 填写后触发 `input` 和 `change` 事件

## 五、话题弹窗添加技术方案

### 5.1 话题添加方式对比

| 发布类型 | 话题添加方式 | 最大数量 |
|---------|-------------|---------|
| 视频发布 | 正文末尾追加 `#话题` | 10 个 |
| 图文发布 | 正文末尾追加 `#话题` | 10 个 |
| **文章发布** | **专用弹窗搜索添加** | **5 个** |

抖音文章发布的话题不是在正文中追加，而是通过一个**独立的添加话题弹窗**来添加。

### 5.2 话题入口结构

```html
<div class="content-obt4oA new-layout-sLYOT6" style="margin-top: 24px;">
  <div>
    <div class="title-dS7kae">
      <span class="title-content-oaqcSp" style="white-space: nowrap;">添加话题</span>
    </div>
  </div>
  <div class="content-child-V0CB7w content-limit-width-zybqBW">
    <div>
      <div>
        <div class="topicSelector-MJsOhh">
          <div class="topicContent-FKzvfX">
            <div class="noTopic-xUe8sb">
              <div class="tagIcon-HKJw9s">
                <span class="tagInnerIcon-n2aot5">#</span>
              </div>
              <div class="tagText-HpPtWq">点击添加话题</div>
            </div>
          </div>
          <div class="topicFooter-FAzpqk">
            <div class="topicCount-rp3vA2">0/5</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
```

### 5.3 弹窗交互流程

```
┌─────────────────────────────────────────────────────────────────┐
│  1) 点击"点击添加话题"区域                                        │
│     - 触发话题搜索弹窗出现                                         │
│     ↓                                                           │
│  2) 在搜索框中输入话题关键词（不带 # 号）                          │
│     - 弹窗内有搜索输入框                                           │
│     - 输入后下方出现话题推荐列表                                    │
│     ↓                                                           │
│  3) 点击推荐列表中的第一个话题项                                    │
│     - 话题项以 # 开头，后面是话题文字                               │
│     - 点击后该话题被添加到已选列表                                  │
│     ↓                                                           │
│  4) 重复步骤 2-3，直到添加完所有话题（最多 5 个）                   │
│     ↓                                                           │
│  5) 点击"确认添加"或点击弹窗外部关闭弹窗                            │
│     - 已添加的话题显示在话题选择器中                                │
└─────────────────────────────────────────────────────────────────┘
```

### 5.4 实现策略（三级降级）

`addArticleTopicsViaPopup` 函数采用三级降级策略：

```
Level 1: JS 方式
  ├─ 点击"添加话题"区域打开弹窗
  ├─ JS 聚焦搜索输入框
  ├─ JS 设置输入框值 + 触发 input 事件
  ├─ JS 点击推荐列表中的第一个话题项
  └─ 循环：下一个话题

Level 2: CDP 输入 + JS 点击
  ├─ CDP 方式输入搜索关键词（更接近真实输入）
  └─ JS 点击推荐项

Level 3: CDP 输入 + 回车确认
  ├─ CDP 方式输入搜索关键词
  └─ CDP 发送 Enter 键确认第一个推荐项
```

### 5.5 话题推荐项识别

推荐项的识别特征：
- 文本以 `#` 开头，后面跟着话题文字
- class 包含 `suggest`、`option`、`result`、`topic-item` 等关键词
- 父容器 class 包含 `suggest`、`dropdown`、`popover`、`topic-list` 等
- 可见性检查：`offsetParent !== null` 且宽高 > 0
- 单行文本，长度 2~50 字符
- 排除干扰项：纯数字、"确认添加"等按钮文本

### 5.6 重要注意事项

1. **搜索关键词不带 `#` 号**：弹窗的搜索框只需要输入纯文字，不需要 `#` 前缀
2. **最多 5 个话题**：抖音文章话题上限是 5 个，不是 10 个
3. **不影响图文/视频发布**：仅文章发布使用弹窗方式，图文/视频仍使用正文追加方式
4. **弹窗关闭**：添加完所有话题后，点击弹窗外部或按 Escape 关闭弹窗

## 六、关键技术点

### 6.1 入口 URL 参数 `default-tab=5`

抖音创作者平台的上传页使用多个 tab：
- `default-tab=1` → 视频上传
- `default-tab=2` → 视频上传（另一种）
- `default-tab=3` → 图文发布
- `default-tab=4` → 合集发布
- `default-tab=5` → **文章（长文）发布**

不同 tab 下，页面会渲染不同的编辑器组件和按钮结构。`default-tab=5` 是文章发布的唯一入口。

### 6.2 引导页"我要发文"按钮

首次进入文章发布页时，可能会出现引导页：
```html
<button class="semi-button semi-button-primary container-drag-btn-hcUuRC">
  <span class="semi-button-content">我要发文</span>
</button>
```
需要检测并点击该按钮才能进入编辑器。

### 6.3 标题 30 字硬限制

抖音文章标题有严格的 30 字符限制：
```typescript
const ARTICLE_TITLE_MAX = 30;
```

### 6.4 正文 8000 字硬限制

抖音文章正文的平台限制是 8000 字符：
```typescript
const ARTICLE_CONTENT_MAX = 8000;
```

### 6.5 React 受控组件的输入方式

抖音文章编辑器的标题和正文字段都是 React 受控组件：
- 直接赋值 `el.value = text` → React 内部 state 不会更新 → 提交时内容被清空
- 必须使用 `document.execCommand('insertText')` 模拟原生用户输入 → 触发 React onChange

### 6.6 自动保存草稿的等待

抖音编辑器会**自动保存草稿**，填写完成后强制 `sleep(2000ms)`，给 React 提交和平台自动保存预留时间。

## 七、平台 meta 配置

`douyin.ts` 中的 meta 信息：

```typescript
export const meta: PlatformMeta = {
  contentLimits: {
    title: 80,      // 视频/图文：标题 80 字
    content: 1000,  // 视频/图文：正文 1000 字
  },
  articleLimits: {
    title: 30,      // 文章：标题 30 字（独立限制）
    content: 8000,  // 文章：正文 8000 字（独立限制）
    summary: 30,    // 文章：摘要 30 字（独立限制）
  },
  contentTypes: ['video', 'image', 'article'],  // 支持三种发布模式
};
```

前端 `Publish.vue` 读取这个 meta，在用户切换到"文章"类型时：
- 标题输入框 maxlength 自动更新为 30
- 正文输入框 maxlength 自动更新为 8000
- 提示语更新为"抖音文章标题 30 字 / 正文 8000 字"
- 账号列表自动过滤掉不支持文章发布的平台（如快手）

## 八、与图文/视频发布的代码隔离

| 模块 | 共享 or 独立 | 说明 |
|------|-------------|------|
| `publishVideo` 方法 | 独立 | 完全独立的函数体，处理视频上传流程 |
| `publishImage` 方法 | 独立 | 完全独立的函数体，处理图文上传流程 |
| `publishArticle` 方法 | 独立 | 完全独立的函数体，处理文章多步发布流程（含封面上传） |
| `uploadArticleCover` 方法 | 独立 | 文章封面专用上传函数，仅被 `publishArticle` 调用 |
| `buildFillTitle` | 共享 | 通用标题填充，字数限制由调用者传入 |
| `buildFillContent` | 共享 | 通用正文填充，字数限制由调用者传入（null 表无限制） |
| `cdpClickPublishButton` | 共享 | CDP 穿透 shadow DOM 通用点击函数 |
| `waitForStable` / 导航跟踪 | 共享 | 所有平台和内容类型共享 |

> 架构原则：每个平台 × 每种内容类型 = 一个独立函数。函数之间只复用底层工具函数（fill, click, navigation-tracking），不共享任何业务流程控制。

## 九、封面上传 7 步流程（uploadArticleCover）

```
[1/5] 滚动到封面设置区域
      - scrollIntoView({ behavior: 'auto', block: 'center' })
      - 验证元素是否在视口内

[2/5] 注入文件上传 patch
      - Page.enable + Page.setInterceptFileChooserDialog（拦截原生对话框）
      - Runtime.enable + executionContextCreated 监听
      - 主文档注入 superPatchScript
      - 遍历所有 iframe，createIsolatedWorld 注入 patch
      - 等待 500ms 让 patch 生效

[3/5] 点击封面上传区域
      - CDP Input.dispatchMouseEvent 真实鼠标事件
      - 多点击点尝试（中心、左上、右下、上中、左中）
      - JS click() 兜底

[4/5] 等待文件设置（CDP兜底）
      - 轮询 40 次（每次 200ms），检查 __flowxCoverFileSet 标记
      - 第15次（约3秒）如果未成功，降级为 CDP DOM.getDocument(pierce:true)
      - 递归遍历所有节点（含 shadow DOM / iframe contentDocument）
      - 使用 DOM.setFileInputFiles 直接设置文件

[5/5] 等待上传并点击完成
      - 发送 Escape 键关闭可能的对话框
      - 轮询 10 次点击"完成"按钮
      - 智能检测无弹窗情况（"编辑封面"文本或背景图）
      - 最后按3次 Escape 确保弹窗关闭
      - 验证封面上传结果
```

## 十、调试日志

```
[douyin][cover-upload] 开始上传文章封面: ...
[douyin][cover-upload] [1/5] 滚动到封面设置区域…
[douyin][cover-upload]   ✅ 封面区域已在视口内
[douyin][cover-upload] [2/5] 注入文件上传 patch…
[douyin][cover-upload]   ✅ Page.enable 成功
[douyin][cover-upload]   ✅ FileChooser 拦截已启用（对话框不会弹出）
[douyin][cover-upload]   ✅ Runtime.enable 成功
[douyin][cover-upload]   ✅ 主文档 patch 注入成功
[douyin][cover-upload]   检测到 1 个子 frame
[douyin][cover-upload]   ✅ frame https://... patch 成功
[douyin][cover-upload]   Patch 注入完成，共 2 个 frame
[douyin][cover-upload] [3/5] 点击封面上传区域…
[douyin][cover-upload]   ✅ 中心 CDP 点击已发送
[douyin][cover-upload] [4/5] 等待文件设置…
[douyin][cover-upload]   ✅ 文件已成功设置（第1次检测，via=js-patch）
[douyin][cover-upload] [5/5] 等待上传并点击完成…
[douyin][cover-upload]   已发送 Escape 键关闭可能的对话框
[douyin][cover-upload]   ✅ 已点击"完成"按钮（尝试1次）
[douyin][cover-upload] 验证封面上传结果…
[douyin][cover-upload]   验证结果: {"hasCover":true,"note":"found-编辑封面"}
[douyin][cover-upload] ✅ 封面上传验证成功
[douyin][upload] ✅ 封面上传成功
```

## 十一、已知限制

| 限制 | 说明 |
|------|------|
| 标题 30 字硬限制 | 由平台后端校验，前端截断无法绕过 |
| 正文 8000 字硬限制 | 由平台后端校验 |
| 摘要 30 字硬限制 | 由平台后端校验 |
| React 受控组件 | 必须用 execCommand 方式输入 |
| 封面图必填 | 抖音文章发布强制要求封面，不上传无法发布 |
| 封面裁剪弹窗 | 部分图片可能弹出裁剪弹窗，代码会自动点击"完成" |
| 自动保存草稿的异步 | 填写完成后需要 ~2 秒等待 |
| 话题最多 5 个 | 文章话题上限是 5 个，少于图文/视频的 10 个 |
| 话题弹窗添加 | 必须通过弹窗搜索添加，不能在正文中追加 |

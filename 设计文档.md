# FlowX 桌面端框架设计文档

> 基于实际代码实现，版本 v1.0。FlowX 是一个多平台内容发布桌面客户端，支持抖音/小红书/快手账号管理与一键发布。

---

## 一、技术选型与架构概述

### 1.1 技术栈

| 层级 | 技术选型 | 版本 | 说明 |
|:---|:---|:---|:---|
| 桌面框架 | Electron | **31.7.7** | 基于 Chromium 126 + Node.js，跨平台支持 |
| 前端框架 | Vue 3 + TypeScript | 3.x | Composition API，类型安全 |
| UI 组件库 | Element Plus | latest | 快速构建后台风格界面 |
| 状态管理 | Pinia | latest | 轻量、模块化，stores 定义在 `src/renderer/stores/` |
| 构建工具 | Vite | latest | 快速冷启动，HMR 支持 |
| 本地存储 | electron-store + safeStorage | latest | 加密存储用户配置、账号 cookie，密钥由操作系统管理 |
| IPC 通信 | ipcMain / ipcRenderer | 内置 | 主进程使用 `ipcMain.handle`，渲染层使用 preload 暴露的 `window.electronApi` |
| 日志系统 | 自研结构化日志（主进程 `console` 写入文件） | - | `stage` 语义化标签便于筛选 |
| 打包 | electron-builder | latest | Windows NSIS 安装包 |
| 平台自动化 | Chrome DevTools Protocol (CDP) + `executeJavaScript` | - | 文件上传用 CDP `DOM.setFileInputFiles`，其余 DOM 操作用 JS 注入 |

### 1.2 架构分层（实际实现）

```
┌─────────────────────────────────────────────────────────────────┐
│                    Renderer Process (Vue 3)                       │
│  ┌───────────────────────┐  ┌───────────────────────┐            │
│  │ AccountPanel.vue      │  │ Publish.vue           │            │
│  │ 账号管理/授权/刷新     │  │ 发布表单/进度/日志     │            │
│  └───────────────────────┘  └───────────────────────┘            │
│  ┌───────────────────────┐  ┌───────────────────────┐            │
│  │ stores/account.ts     │  │ stores/publish.ts     │            │
│  │ 账号状态/IPC 事件监听   │  │ 任务状态/自动移除       │            │
│  └───────────────────────┘  └───────────────────────┘            │
└────────────────────────┬──────────────────────────────────────────┘
                         │ contextBridge (preload/index.ts)
                         │ window.electronApi
┌────────────────────────▼──────────────────────────────────────────┐
│                    Electron Main Process                           │
│  ┌───────────────────────┐  ┌───────────────────────┐            │
│  │ AccountService.ts     │  │ PublishEngine.ts      │            │
│  │ 账号授权/cookie 注入   │  │ 并发控制/状态汇总/IPC  │            │
│  │ DOM 信息提取/加密存储  │  │ 推送                  │            │
│  └───────────────────────┘  └───────────────────────┘            │
│  ┌───────────────────────────────────────────────────────┐        │
│  │ PlatformAdapter.ts + platforms/shared.ts              │        │
│  │ 浏览器窗口创建 / 导航跟踪器 / CDP 文件上传 /           │        │
│  │ evalJS 重试执行器 / 标题正文填充 / 发布按钮点击器     │        │
│  │ ┌─────────┐  ┌──────────┐  ┌──────────┐              │        │
│  │ │ douyin  │  │ xiaohongshu│  │kuaishou │ ... (扩展)  │        │
│  │ └─────────┘  └──────────┘  └──────────┘              │        │
│  └───────────────────────────────────────────────────────┘        │
│  ┌───────────────────────┐  ┌───────────────────────┐            │
│  │ ipc/account.ts        │  │ ipc/publish.ts        │            │
│  │ 账号相关通道注册       │  │ 发布相关通道注册       │            │
│  └───────────────────────┘  └───────────────────────┘            │
│  ┌───────────────────────────────────────────────────────┐        │
│  │ index.ts — 应用入口                                    │        │
│  │ 单实例锁 / GPU 加速策略 / 窗口创建 / IPC 注册          │        │
│  └───────────────────────────────────────────────────────┘        │
│  ┌───────────────────────────────────────────────────────┐        │
│  │ store/SecureStore.ts — 加密本地存储（账号/任务）       │        │
│  └───────────────────────────────────────────────────────┘        │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 核心设计原则

| 原则 | 描述 |
|:---|:---|
| **账号隔离** | 每个账号使用独立的 Electron `session` partition（`persist:account_{id}`），天然隔离登录态，避免平台互相踢下线 |
| **适配器模式** | 平台差异抽象为 `StandardPublishConfig`，共享 `runStandardPublish` 流程。修改一个平台不会影响其他平台 |
| **导航稳定性优先** | 所有 DOM 操作前检查 `isLoading` + 导航静默期，避免"页面正在跳转时执行 JS 导致 frame disposed" |
| **崩溃自动恢复** | 发布窗口的渲染进程崩溃（`render-process-gone`）后自动 reload 1 次（Electron 31 已从根源解决，此处为兜底） |
| **分层可见性** | `AccountCredential`（含 cookie）仅在主进程存在，IPC 返给前端的是脱敏的 `AccountInfo` |
| **IPC 安全** | 渲染层通过 preload 暴露的白名单 API 与主进程通信；所有外部 URL 的窗口不接收 IPC 消息 |

---

## 二、目录结构（实际文件）

```
flowx-desktop/
├── src/
│   ├── main/                              # Electron 主进程
│   │   ├── index.ts                       # 应用入口：GPU 策略、单实例锁、服务初始化
│   │   ├── windows/
│   │   │   ├── MainWindow.ts              # 主窗口创建
│   │   │   └── AccountBrowserView.ts      # 账号创作中心窗口
│   │   ├── ipc/
│   │   │   ├── index.ts                   # 统一通道注册（safeInvoke 封装）
│   │   │   ├── account.ts                 # 账号相关 IPC
│   │   │   ├── publish.ts                 # 发布相关 IPC
│   │   │   └── system.ts                  # 系统信息/日志路径
│   │   ├── services/
│   │   │   ├── AccountService.ts          # 账号授权、信息提取、cookie 管理
│   │   │   ├── PublishEngine.ts           # 发布引擎：队列/并发/IPC 推送（通过 PlatformDispatcher 调用平台）
│   │   │   ├── PlatformAdapter.ts         # 平台适配器接口（deprecated，向后兼容）
│   │   │   └── platforms/
│   │   │       ├── types.ts               # PlatformAdapter 接口定义 / PublishExecutor
│   │   │       ├── registry.ts            # 平台注册表（registerPlatform / getPlatformAdapter）
│   │   │       ├── PlatformDispatcher.ts  # 工厂方法分发器: createExecutor(platform, contentType)
│   │   │       ├── shared.ts              # 核心：窗口创建/导航跟踪器/CDP 上传/evalJS 重试器/按钮点击器/标题填充器/runStandardPublish
│   │   │       ├── index.ts               # 所有平台的统一导入入口（隐式注册）
│   │   │       ├── douyin.ts              # 抖音平台实现（detectLoggedIn/extractPageInfo/publishVideo/publishImage/publishArticle）
│   │   │       ├── xiaohongshu.ts         # 小红书平台实现（同上，粉丝数/关注数/获赞数解析）
│   │   │       └── kuaishou.ts            # 快手平台实现（contenteditable / user-cnt__item 解析）
│   │   ├── store/
│   │   │   └── SecureStore.ts             # electron-store + safeStorage 封装
│   │   └── utils/
│   │       └── logger.ts                  # 统一日志格式
│   ├── preload/
│   │   └── index.ts                       # contextBridge 暴露 electronApi
│   ├── renderer/                          # Vue 3 前端
│   │   ├── App.vue
│   │   ├── main.ts
│   │   ├── pages/
│   │   │   ├── Dashboard.vue              # 首页仪表盘
│   │   │   ├── AccountPanel.vue           # 账号管理（授权/刷新/删除/打开创作中心）
│   │   │   └── Publish.vue                # 发布页面（表单/账号卡片选择/进度面板/调试日志）
│   │   └── stores/
│   │       ├── account.ts                 # 账号列表状态（Pinia）
│   │       └── publish.ts                 # 发布任务状态（Pinia，含 IPC 事件订阅/自动移除定时器）
│   └── types/
│       └── index.ts                       # 全局共享类型（PlatformMeta/AccountInfo/PublishRequest/PublishItemProgress/...）
├── electron-builder.yml                   # 打包配置
├── vite.config.ts                         # Vite 配置（含 vite-plugin-electron）
├── package.json                           # Electron 31.7.7
└── README.md
```

---

## 三、账号服务（AccountService）

### 3.1 授权流程（核心）

用户在 AccountPanel.vue 点击"新增账号" → 选择平台 → 开始授权：

```
startAuth(platform)
  └─> ipc account:beginAuth
        └─> AccountService.beginAuthorization(platform)
              │
              │ [STEP 1] 账号窗口创建
              │   new BrowserWindow({ partition: `persist:account_${id}`, ... })
              │   loadURL(platform.authUrl)
              │
              │ [STEP 2] 监听保存信号（三重机制）
              │   ① page-title-updated 事件（页面标题变化 → 可能已登录）
              │   ② did-navigate-in-page hash 变化（SPA 路由）
              │   ③ 500ms 轮询 executeJavaScript 检测登录关键字
              │
              │ [STEP 3] 检测登录态（调用平台适配器 detectLoggedIn）
              │   最多重试 2 次，等待 2s 后检测
              │
              │ [STEP 4] DOM 信息提取
              │   nickname / avatar / platformAccountId / fansCount / followCount / likeCount
              │   由平台适配器 extractPageInfo 实现（详见 platforms/xiaohongshu.ts §3.2）
              │
              │ [STEP 5] cookie 提取 + 校验
              │   ses.cookies.get({ url: undefined }) 提取所有 cookie
              │   ▸ 黑/白名单双重校验：
              │     · 必须有真实昵称或平台账号 ID（过滤"抖音创作者中心"等占位）
              │     · cookies 数量 ≥ 5（空账号只有少量 session cookie）
              │     · 必须包含 session/token/a1/sec_user_id 等关键字段之一
              │
              │ [STEP 6] 账号去重
              │   同一 platform + platformAccountId / userId 命中 → 更新旧记录
              │   否则 → 新增记录
              │
              └─ 加密写入 SecureStore，返回 AccountInfo
```

### 3.2 Cookie 注入机制

```typescript
// injectAccountCookies(accountId, targetUrl?)
// 1. 获取目标 partition
// 2. 从 AccountCredential 读取 cookies[]
// 3. 逐个 set({ url: cookie.url 或 targetUrl, ... })
// 4. 统计 injected / skipped / failed
// 5. 跳过反自动化 cookie：acw_tc / ttwid / _csrf / _x_b / xsec_
//    （这些是平台反自动化系统标记，每次打开新浏览器窗口都会重新生成）
```

使用场景：
- **账号刷新**：先用旧 partition 打开 homeUrl，重新检测 + 提取 + 覆盖保存
- **发布前**：在发布窗口的 partition 中注入账号 cookies，使页面以已登录状态加载创作中心
- **打开创作中心**：用户手动打开某个账号的创作中心查看数据

### 3.3 账号信息字段

```typescript
AccountInfo {
  id: string;                      // 本地唯一 ID（task_{random}）
  platform: PlatformType;          // 'douyin' | 'xiaohongshu' | 'kuaishou'
  nickname: string;                // DOM 提取
  avatar?: string;                 // DOM 提取
  platformAccountId?: string;      // DOM 提取（抖音号/小红书账号）
  userId?: string;                 // cookie 提取（a1 / sec_user_id / open_id 等）
  fansCount?: number;              // DOM 提取（粉丝数）
  followCount?: number;            // DOM 提取（关注数）
  likeCount?: number;              // DOM 提取（获赞与收藏）
  status: 'active' | 'expired' | 'disabled';
  authorizedAt: number;
  expiresAt?: number;              // heuristic: authorizedAt + 14天
  remark?: string;                 // 用户备注（保存在 __flowx_remark__ cookie）
  capabilities: AccountCapabilities;  // { publishVideo, publishImage, publishArticle }
}
```

---

## 四、发布引擎（PublishEngine）

### 4.1 发布任务数据流

```
Publish.vue submitPublish()
  │
  ├─> publishStore.submit(request)      // 前端登记 queued 状态
  │     └─> electronApi.submitPublish(request)
  │
  │ [主进程]
  └─> PublishEngine.submit(req)
        │
        ├─ 生成 taskId = `task_{6位随机}`
        ├─ 展开为 PublishItemProgress[]（每个 accountId 一条）
        ├─ 登记到 running Map
        ├─ ✅ 立即推送一次 status=queued 到渲染层（避免点击没反应）
        │
        ├─ 若有 scheduledAt → setTimeout(runTask, delay)
        └─ 否则立即 runTask(task)
              │
              ├─ drain() 循环：activeCount < concurrency 时执行 item
              ├─ runItem(taskId, accountId)
              │    ├─ 校验账号存在/未过期（失败直接 failed）
              │    ├─ injectAccountCookies(accountId)
              │    └─ adapter.publish(accountId, request, onProgress)
              │         └─> platforms/shared.ts runStandardPublish()
              │
              └─ 每次 onProgress 回调 → notifyStatus(task, verbose=true)
                   └─> ipc publish:statusChanged → publishStore._applyStatusUpdate
                         └─> Publish.vue 折叠面板实时刷新
```

### 4.2 并发控制

```typescript
let concurrency = 3;        // 可通过 publish:setConcurrency 动态调整（1~20）
let activeCount = 0;        // 正在执行的 item 数
let queue: Array<{ taskId, accountId }>;  // 等待队列

function drain() {
  while (activeCount < concurrency && queue.length > 0) {
    const next = queue.shift()!;
    activeCount++;
    runItem(next.taskId, next.accountId).finally(() => {
      activeCount--;
      setImmediate(drain);     // 尾递归：释放事件循环后继续调度
    });
  }
}
```

**为什么并发数默认是 3**：每个发布 item 都打开一个独立的 BrowserWindow（含完整的 Chromium 实例），内存和 CPU 占用与窗口数线性相关。3 并发在普通办公电脑（16GB RAM）上保持稳定，更多窗口会触发系统级 swap 导致所有窗口都变慢。

### 4.3 发布状态（PublishStatus）

```
queued → running → success
              → failed
              → cancelled
              → scheduled
```

- `running` 占 0~95%（上传/填写/点击各阶段递增）
- `success` = 100%，显示绿色
- `failed` 保留当前进度并显示错误消息，窗口保持打开供用户查看原因
- `scheduled` 为定时发布，时间到后自动开始

### 4.4 IPC 推送通道设计

主进程 `PublishEngine.notifyStatus` → 渲染层 `stores/publish.ts _applyStatusUpdate`，是唯一的状态同步路径。

**关键安全过滤**：在发送 IPC 前，`notifyStatus` 遍历 `BrowserWindow.getAllWindows()`，对每个 webContents 检查 URL：
- 外部 URL（`http(s)://`）→ **跳过**。把 IPC 消息发到抖音创作者中心页面会触发 `Render frame was disposed before WebFrameMain could be accessed`
- 本地 URL（`file://` / `chrome-error://`）→ 正常推送
- 发布窗口通过 `win.webContents.userAgent` + `_flowxPublishWindow` 标识位双重识别

这是防止"发布成功后主窗口卡死/白屏"的关键机制。

### 4.5 重启恢复

应用启动时 `PublishEngine.init()` 从磁盘读取 `publishTasks`（最近 200 条）。若其中存在 `queued/running` 状态的任务（说明上次应用被强制关闭），统一重置为 `failed`，保证数据一致性。

---

## 五、平台适配器（PlatformDispatcher + 工厂方法 + 适配器模式）

这是整个项目最核心的模块，承担 90% 的平台自动化逻辑。

### 5.1 架构目标与原则

| 原则 | 说明 |
|------|------|
| **单一职责** | 每个平台文件只负责自己的 DOM 适配（标题选择器、按钮定位、粉丝数解析等），绝不包含其他平台的逻辑 |
| **平台互不影响** | 修改快手的提取逻辑绝不会影响小红书/抖音 —— 三个平台在独立文件中实现，通过注册表松散耦合 |
| **标准化流程** | 90% 的发布流程是相同的（窗口/导航/登录/上传/填写/点击/验证），通过 `shared.ts` 的 `runStandardPublish` 统一实现 |
| **两级分发** | 第一级按平台（kuaishou/xiaohongshu/douyin），第二级按内容类型（video/image/article） |

### 5.1 适配器模式（扩展：按内容类型分发）

```typescript
// PlatformDispatcher.ts — 工厂方法
export function createExecutor(
  platform: string,                 // 'kuaishou' | 'xiaohongshu' | 'douyin'
  contentType: 'video' | 'image' | 'article',
): PublishExecutor {
  const adapter = getPlatformAdapter(platform);
  // 优先调用精确方法（publishVideo/publishImage/publishArticle）
  // 降级到通用 publish（向后兼容）
  switch (contentType) {
    case 'video':   return { execute: adapter.publishVideo || adapter.publish };
    case 'image':   return { execute: adapter.publishImage || adapter.publish };
    case 'article': return { execute: adapter.publishArticle || adapter.publish };
  }
}

// PublishEngine.ts 使用
const executor = PlatformDispatcher.createExecutor(platform, req.contentType);
return executor.execute(accountId, req, onProgress);
```

每个平台只需实现 PlatformAdapter 接口（详见 platforms/types.ts）。`runStandardPublish` 是所有平台共享的标准流程。

### 5.2 平台独立实现 + shared.ts 通用工具

v1.1 架构重大调整：**平台差异彻底隔离到各自的 .ts 文件**，`shared.ts` 不再包含任何平台特定的选择器/关键字，只提供通用工具函数。

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    shared.ts — 通用工具层（无平台逻辑）              │
│  - makePublishWindow()       创建发布窗口（统一参数/监听）           │
│  - attachNavigationTracker() 导航跟踪（所有平台共用）                │
│  - evalJS(win, code, desc)   executeJavaScript 封装（6次重试/退避） │
│  - uploadViaCDP(win, files)   CDP DOM.setFileInputFiles 文件上传    │
│  - cdpClickPublishButton()    CDP 穿透 closed shadow DOM 点击       │
│  - makeFailedResult()         构造失败返回对象                       │
│  - sleep()                    等待                                    │
└─────────────────────────────────────────────────────────────────────────┘
           ▲           ▲           ▲
           │           │           │
┌──────────┴──┐ ┌──────┴────────┐ ┌──────────┴────────┐
│ xiaohongshu│ │  douyin.ts     │ │  kuaishou.ts       │
│ .ts         │ │               │ │                    │
│ publishVideo│ │ publishVideo   │ │ publishVideo       │
│ publishImage│ │ publishImage   │ │ publishImage        │
│ publishArtic│ │ publishArticle │ │ publishArticle      │
│ 平台独有逻辑│ │ 平台独有逻辑   │ │ 平台独有逻辑        │
│ ProseMirror │ │ primary-button │ │ _button-primary_   │
│ xhs-publish│ │ <button> class │ │ 标签最多4个         │
│ -btn shadow │ │               │ │                    │
└─────────────┘ └───────────────┘ └────────────────────┘
```

每个平台文件的**标准流程**：

```
Phase 1: 初始化
  └─> makePublishWindow() / attachNavigationTracker()  [来自 shared.ts]

Phase 2: 导航到正确的发布页（**平台独有**）
  快手：loadURL('https://cp.kuaishou.com/article/publish/video?tabType=2')
  抖音：loadURL('https://creator.douyin.com/creator-micro/content/upload?default-tab=3')
  小红书：loadURL('https://creator.xiaohongshu.com/publish/publish')
  └─ tracker.waitForStable(1500, 15000)

Phase 3: 登录态检测（**平台独有**）
  └─> evalJS(win, detectLoggedInScript, '检测登录态')
       · 快手：检查 body 包含 "发布作品" 关键字
       · 抖音：检查 URL 包含 creator.douyin.com
       · 小红书：检查 body 包含 "发布笔记" / "创作服务平台"

Phase 4: 文件上传（通用 + contentType 区分）
  └─ uploadViaCDP(win, files, log, contentType='image' | 'video')
       · CDP DOM.getDocument(pierce:true) 深度遍历
       · 按 accept 属性排序（image/jpeg vs video/mp4）
       · DOM.setFileInputFiles(nodeId, files)
       · 手动派发 change/input 事件（React setState）

Phase 5: 上传完成轮询（通用 waitForUploadComplete）
  └─ evalJS(win, 'status:' + status + ' hasThumb:' + ...)

Phase 6: 内容填写（**平台独有脚本函数**）
  每个平台实现自己的 build 函数：
    · buildFillTitleScript(title)       — 标题输入框定位 + execCommand 填写
    · buildFillContentScript(content)    — 正文 contenteditable / ProseMirror
    · buildClickPublishScript()          — 发布按钮点击（精确匹配）
  注意：ProseMirror 的编辑区不接受 .value = ''，
        必须用 document.execCommand('insertText') 模拟原生输入。

Phase 7: 点击发布（平台独有 + CDP 穿透兜底）
  └─ evalJS(win, buildClickPublishScript(), '点击发布')
  └─ 如果 JS 点击无效（shadow DOM 情况）：
       cdpClickPublishButton(win, log, ['发布笔记', '发布', '立即发布'])

Phase 8: 发布后验证（检测跳转到作品管理页 / 文本成功关键字）

Phase 9: 完成/清理
  └─ success → win.destroy() 自动关闭
      failed → 保持窗口打开，让用户查看错误信息或手动发布
```

### 5.3 PlatformDispatcher 两级分发

```typescript
// PlatformDispatcher.ts
// Level 1: platform (xiaohongshu | douyin | kuaishou)
// Level 2: contentType (video | image | article)

function createExecutor(platform, contentType) {
  const adapter = getPlatformAdapter(platform);
  switch (contentType) {
    case 'video':   return adapter.publishVideo;   // ← 平台独立实现
    case 'image':   return adapter.publishImage;   // ← 平台独立实现
    case 'article': return adapter.publishArticle; // ← 平台独立实现
  }
}
```

**核心设计目标**：修改快手的图文发布逻辑 → 不需要查看/修改抖音/小红书文件。

### 5.4 图文 vs 视频的关键差异

| 差异点 | 视频发布 | 图文发布 |
|-------|---------|---------|
| 上传 input | `accept="video/*"` | `accept="image/png, image/jpeg"` |
| 标题 | `<input type="text" placeholder="添加作品标题">` | `<input type="text" placeholder="填写标题会有更多赞哦">` |
| 正文 | `contenteditable=true` 简单文本 | ProseMirror / Tiptap 富文本编辑器 |
| 标签 | 抖音 10个 / 快手 4个 | 小红书话题（#xxx） |
| 发布按钮 | `<button class="_button-primary_">发布作品</button>` | `<xhs-publish-btn> shadow DOM` |
| 字数限制 | 抖音 5000字 / 快手 500字 | 小红书 1000字 |

### 5.5 ProseMirror 富文本处理（小红书图文）

小红书正文编辑区是 ProseMirror/Tiptap：

```html
<div class="ProseMirror" contenteditable="true" role="textbox">
  <p>第一段</p>
  <p>第二段</p>
</div>
```

写入策略：
1. 先 `document.execCommand('selectAll')` 选中现有内容
2. `document.execCommand('delete')` 清空
3. **逐行** `document.execCommand('insertText', false, text + '\n')`
4. 让 ProseMirror 通过监听 `input` 事件更新自己的 state

直接设置 `.innerText = content` 或 `.textContent = content` 会失败，因为 ProseMirror 在下次 keydown 时会还原 state。

### 5.6 Closed Shadow DOM 穿透点击（小红书发布按钮）

小红书的发布按钮是自定义 Web Component：

```html
<xhs-publish-btn>    <!-- host element (visible to JS) -->
  <!-- Shadow Root (内部 closed，JS 无法访问) -->
  #shadow-root (closed)
    <div>
      <button>发布笔记</button>   <!-- 真正的按钮 -->
    </div>
</xhs-publish-btn>
```

**为什么 JS click() 不生效**：`element.click()` 只在 host 元素上触发，但事件监听器绑定在 shadow DOM 内部的 `<button>` 上。

**CDP 解决**：使用 Chrome DevTools Protocol `DOM.getDocument(depth: -1, pierce: true)`
- `pierce:true` = 强制穿透 closed shadow DOM
- 返回结果包含 shadow DOM 中的所有节点
- 遍历找到 `<button>` 或 `<div role="button">`，计算其坐标
- 用 `Input.dispatchMouseEvent(type: mouseMoved → mousePressed → mouseReleased)` 模拟真实鼠标点击

```typescript
// shared.ts cdpClickPublishButton(win, log, keywords)
1. CDP DOM.getDocument({ depth: -1, pierce: true })   // ← 关键：pierce:true
2. 遍历树，收集:
   - button 元素（含 shadow DOM 内）
   - a 元素
   - div[role=button] / span[role=button]
   - 含关键字文本的元素
3. DOM.getBoxModel(nodeId) → 计算中心坐标 (x, y)
4. Input.dispatchMouseEvent
   · type: 'mouseMoved'
   · type: 'mousePressed'
   · type: 'mouseReleased'
5. 等待 2-3 秒检测 URL 是否从 /publish → /manage（表示发布成功）
```

### 5.7 React 受控组件：标题填写时机问题

如果在填写标题后立即填写正文（都在 React 页面中），会遇到以下问题：

```
标题 input (受控组件):
  state: { title: '' }
  onChange: e => setState({ title: e.target.value })
  value: this.state.title         ← 渲染出来的 value 永远跟 state 同步

时序:
  t1: JS 设置 input.value = '我的标题'   ← 改了 DOM，没改 state
  t2: React 重新渲染（其他字段更新时）  ← 重新渲染 input，state 还是 ''
  t3: input.value = ''                   ← 标题被清空！
```

**解决**：用 `document.execCommand('insertText')` 代替直接赋值：

```javascript
input.focus();
document.execCommand('selectAll');
document.execCommand('delete');
document.execCommand('insertText', false, '我的标题');
// → execCommand 会触发原生 input 事件，React 监听该事件
// → 调用 onChange(e)，正确更新 state.title = '我的标题'
// → 下次重渲染保持正确值
```

### 5.8 导航跟踪器（NavigationTracker）

**问题背景**：发布流程中，文件上传后抖音/xhs 会触发 SPA 跳转到内容编辑页，此时立即 `executeJavaScript` 会失败，原因是：
- `Render frame was disposed before WebFrameMain could be accessed`
- `document.body is null`
- `context is invalid`

**解决方案**：在发布窗口创建时就挂接 `NavigationTracker`，每次执行 DOM 操作前调用 `tracker.isNavigating()` 或 `tracker.waitForStable()`。

```typescript
// 内部实现思路
lastNavigationAt: number;      // 最近一次导航事件的时间
listeners: Array<() => void>;  // 监听句柄，dispose 时清理

// 监听事件：did-navigate / did-navigate-in-page / did-frame-navigate / did-start-loading / did-stop-loading
// 每次触发 → lastNavigationAt = Date.now()

isNavigating() {
  return Date.now() - lastNavigationAt < 500 || win.webContents.isLoading();
}

waitForStable(minStableMs = 1000, timeoutMs = 10000) {
  while (Date.now() - start < timeoutMs) {
    if (!isNavigating() && Date.now() - lastNavigationAt >= minStableMs) return;
    await sleep(200);
  }
  log('warn', 'navigation', `稳定超时 ${timeoutMs}ms，继续后续流程`);
}
```

### 5.4 evalJS 重试执行器

封装 `win.webContents.executeJavaScript`，带 **最大 6 次重试**和**智能退避**：

```typescript
function isTransientEvalError(msg: string): boolean {
  return /frame was disposed|Render frame|navigation|No current|Cannot read property|
          document.body is null|null object|context is invalid|Execution context was destroyed|
          No frame|target frame|subframe/i.test(msg);
}

for (let attempt = 0; attempt < maxRetries; attempt++) {
  try {
    const result = await win.webContents.executeJavaScript(code);
    if (attempt > 0) log('debug', 'eval', `${desc} 重试成功（第${attempt+1}次）`);
    return result;
  } catch (err) {
    const msg = err.message;
    if (isTransientEvalError(msg) && attempt < maxRetries - 1) {
      const backoff = Math.min(2000, 250 * (attempt + 1));
      await sleep(backoff);
      continue;
    }
    throw err;
  }
}
```

### 5.5 React 受控组件兼容

抖音/xhs 的标题输入框都是 React `useState` 受控组件。直接 `el.value = 'xxx'` 不生效，因为 React 内部 state 没变，下次 re-render 就会被还原。

**解决方案**：

```javascript
// 1. 通过原生 setter 赋值（绕过 React 的 value setter 拦截）
const _desc = Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value');
if (_desc && _desc.set) _desc.set.call(el, value);

// 2. 派发 React 监听的原生事件
el.dispatchEvent(new Event('input', { bubbles: true }));
el.dispatchEvent(new Event('change', { bubbles: true }));
el.dispatchEvent(new Event('blur', { bubbles: true }));
```

`buildFillTitle` / `buildFillContent` 都使用此模式，并会按评分排序候选元素：
- **标题**：关键词 `标题/title/作品标题` + 输入框 tag + 高度评分
- **正文**：关键词 `描述/正文/内容/简介/description` + textarea/contenteditable + 排除"标题"字段
- **话题**：用户输入的 `tag1, tag2` 自动解析为 `#tag1 #tag2` 行文本，附加在内容末尾

### 5.6 文件上传（CDP setFileInputFiles）

Electron 默认限制：从渲染进程的 JS 无法直接设置 `<input type=file>` 的 `files`（浏览器安全策略）。

**解决方案**：使用 Chrome DevTools Protocol 的 `DOM.setFileInputFiles` 命令：

1. 通过 `debugger` 协议附加到发布窗口的 webContents
2. `DOM.getDocument` 获取整棵 DOM 树
3. 查找第一个 `<input type="file">` 的 `nodeId`
4. `DOM.setFileInputFiles({ nodeId, files: [path1, path2, ...] })` 注入本地文件路径
5. 在页面上模拟 change 事件（让 React 感知到上传了文件）
6. 断开 debugger 连接

这种方式是自动化场景的标准做法，Selenium/Puppeteer 内部也是同样机制。

---

## 六、反崩溃策略（重点：抖音发布场景）

### 6.1 问题背景

在 Electron 28 上运行抖音发布流程时，文件上传完成后约 2~5 秒内会触发渲染进程崩溃：

```
[ERROR:CONSOLE(1)] exitCode: -1073741819 (STATUS_ACCESS_VIOLATION)
"Render frame was disposed before WebFrameMain could be accessed"
```

**根因**：抖音创作者中心在视频上传后会：
1. 启动 `WebAssembly` 模块进行人脸检测/封面推荐（`bytenn.wasm`）
2. 在 `WebWorker` 中做视频帧提取和解码
3. 使用 `WebGL` 渲染预览画面
4. 尝试硬件加速视频解码（GPU 路径）

这几条路径在 Electron 28 + Windows 组合下（尤其是无沙盒环境）都不稳定，容易触发 `STATUS_ACCESS_VIOLATION`。

### 6.2 防御层次（最终方案：Electron 31）

| 层次 | 位置 | 说明 |
|:---|:---|:---|
| 1. **Electron 版本升级** | `package.json` | 从 28 → 31.7.7（Chromium 126）。Chromium 官方修复了大量无沙盒环境下 GPU/Worker 相关的稳定性问题 |
| 2. **全局 GPU 禁用** | `src/main/index.ts` | `app.disableHardwareAcceleration()` 全局关闭硬件加速 |
| 3. **Chromium 命令行开关** | `src/main/index.ts` | `disable-gpu-sandbox` / `disable-gpu-compositing` / `disable-accelerated-video-decode` / `disable-accelerated-video-encode` / `disable-webgl` |
| 4. **软件 GL 回退** | `src/main/index.ts` | `use-gl=swiftshader`，GPU 禁用后使用软件渲染 |
| 5. **V8 堆内存** | `src/main/index.ts` | `js-flags=--max-old-space-size=8192`，防止创作中心大页面 OOM |
| 6. **崩溃自动恢复** | `platforms/shared.ts` | `render-process-gone` 事件 → `win.reload()` 自动 reload 1 次（兜底） |

### 6.3 为什么升级到 31 是最终方案

- Chromium 112 (Electron 28) → 126 (Electron 31) 之间跨越 14 个主要版本
- 官方 Release Notes 中 GPU、WebAssembly、Worker 相关的修复有数十项
- 在相同测试场景下：Electron 28 崩溃率 ≈ 80%，Electron 31 崩溃率 ≈ 0%
- 升级后可以去掉大量激进的 polyfill 代码（如冻结 WebAssembly/Worker），减少对页面的干扰，让视频上传的 WebAssembly 流程正常工作

### 6.4 注意事项

- `app.disableHardwareAcceleration()` **必须在 app ready 前调用**，否则 Chromium 已经启动 GPU 进程后无法回退
- 发布速度会变慢（软件渲染），但整体稳定性大幅提升
- 对正常使用（用户手动在浏览器窗口操作）完全无影响

---

## 七、账号信息提取（平台差异化）

### 7.1 统一流程

`AccountService.beginAuthorization` / `AccountService.refreshToken` 都调用同一流程：

1. 等待 `homeUrl` 加载完成
2. `platformAdapter.extractPageInfo(win)` → 返回 `{ nickname, avatar, platformAccountId, fansCount?, followCount?, likeCount? }`
3. `platformAdapter.getUserId(cookies)` → 从 cookies 反查 userId
4. 写入 SecureStore

### 7.2 平台差异点

| 平台 | 昵称选择器 | 头像选择器 | 粉丝/关注/获赞 | userId 字段 |
|:---|:---|:---|:---|:---|
| 抖音 | `.user-info-name` / 页面 title | `.avatar img` | `textContent 匹配 "粉丝 N / 关注 N / 获赞 N"` | `sessionid` 对应 `uid` |
| 小红书 | `account-name` / `.nickname` | `img[class*=avatar]` | 精确 DOM：`<span class="numerical">N</span>` 后跟标签"粉丝数/关注数/获赞与收藏"（详见 xiaohongshu.ts） | `a1` cookie |
| 快手 | 页面标题 / `.user-name` | `.user-avatar img` | textContent 正则扫描 | `kuaishou.server.web_ph`

### 7.3 小红书粉丝数解析的特殊处理

小红书使用数字 + 标签分离的 DOM 结构：
```html
<div>
  <span class="numerical">16</span>   <span>关注数</span>
  <span class="numerical">12</span>   <span>粉丝数</span>
  <span class="numerical">62</span>   <span>获赞与收藏</span>
</div>
```

关键修复点（详见 `platforms/xiaohongshu.ts §extractPageInfo`）：
- 使用 `numerical` class 精确定位数字元素
- 按 DOM 顺序匹配后面的第一个标签（"粉丝数"/"关注数"/"获赞与收藏"）
- 支持 "1.2万" 格式数字 → `parseFloat + 10000` 倍率
- 全文扫描兜底：`/(粉丝|关注|获赞)[^\d\n]*(\d[\d,，万wWkK千]*)/i`

---

## 八、安全设计

### 8.1 Sandbox + Context Isolation

```typescript
// 所有 BrowserWindow（主窗口 + 发布窗口 + 授权窗口）
new BrowserWindow({
  webPreferences: {
    sandbox: true,
    contextIsolation: true,
    nodeIntegration: false,
    nodeIntegrationInWorker: false,
    partition: `persist:account_${id}`, // 额外的账号隔离
  },
});
```

即使主窗口页面被攻破，也无法：
- 访问 Node.js API
- 访问其他账号的 cookies（partition 隔离）
- 向外部 URL 发送 IPC 消息（主进程 `notifyStatus` 对外部 URL 窗口做了过滤）

### 8.2 Preload 白名单 API

```typescript
// preload/index.ts 暴露的 API
window.electronApi = {
  // 账号
  listPlatforms: () => ipcRenderer.invoke('account:listPlatforms'),
  listAccounts: () => ipcRenderer.invoke('account:list'),
  beginAuth: (platform) => ipcRenderer.invoke('account:beginAuth', platform),
  refreshToken: (id) => ipcRenderer.invoke('account:refresh', id),
  deleteAccount: (id) => ipcRenderer.invoke('account:delete', id),
  updateAccount: (id, data) => ipcRenderer.invoke('account:update', id, data),
  openCreator: (id) => ipcRenderer.invoke('account:openCreator', id),

  // 发布
  submitPublish: (req) => ipcRenderer.invoke('publish:submit', req),
  getPublishProgress: (taskId) => ipcRenderer.invoke('publish:progress', taskId),
  listTasks: () => ipcRenderer.invoke('publish:list'),
  cancelTask: (taskId) => ipcRenderer.invoke('publish:cancel', taskId),
  setConcurrency: (n) => ipcRenderer.invoke('publish:setConcurrency', n),
  onPublishStatusChanged: (callback) => {
    const handler = (_e, data) => callback(data);
    ipcRenderer.on('publish:statusChanged', handler);
    return () => ipcRenderer.removeListener('publish:statusChanged', handler);
  },

  // 文件
  openFileDialog: (opts) => ipcRenderer.invoke('system:openFileDialog', opts),

  // 日志
  getLogPaths: () => ipcRenderer.invoke('system:getLogPaths'),
  openLogDir: () => ipcRenderer.invoke('system:openLogDir'),
  queryLogs: (query) => ipcRenderer.invoke('publish:queryLogs', query),
  clearLogs: () => ipcRenderer.invoke('publish:clearLogs'),

  // 系统
  getSystemInfo: () => ipcRenderer.invoke('system:getInfo'),
  getAppVersion: () => ipcRenderer.invoke('system:getAppVersion'),
};
```

白名单策略：渲染层只能调用显式暴露的函数，每个函数对应一个 IPC 通道，主进程 handler 做参数校验。

### 8.3 Cookie 加密存储

账号凭证包含平台 session cookie，属于高度敏感数据。

```typescript
// SecureStore.ts 封装
import Store from 'electron-store';
import { safeStorage } from 'electron';

const store = new Store({
  name: 'flowx-secure',
  encryptionKey: 'flowx-desktop-v1',  // 加密文件名（防止本地明文扫描）
});

// 保存：先通过 safeStorage.encryptString 加密，再写入
export function saveCredentials(acc: AccountCredential) {
  if (safeStorage.isEncryptionAvailable()) {
    acc.cookies = acc.cookies.map(c => ({
      ...c,
      value: safeStorage.encryptString(c.value).toString('base64'),
    }));
  }
  store.set(`account.${acc.id}`, acc);
}
```

**加密级别**：
- Windows：DPAPI（Data Protection API），绑定当前 Windows 用户
- macOS：Keychain
- Linux：libsecret

攻击者即使拿到 `flowx-secure.json` 文件，在不同机器/不同用户账号下也无法解密。

### 8.4 IPC 推送的窗口过滤（关键安全点）

`PublishEngine.notifyStatus` 的实现细节：

```typescript
// 向主窗口推送状态，但跳过加载外部 URL 的窗口（如抖音创作中心）
for (const win of BrowserWindow.getAllWindows()) {
  const wc = win.webContents;
  const url = wc.getURL();
  if (url.startsWith('http://') || url.startsWith('https://')) continue;  // 跳过外部页
  if (wc.hostWebContents) continue;                                        // 跳过 iframe 宿主
  wc.send('publish:statusChanged', payload);
}
```

**为什么需要过滤**：
- 当主窗口是 `file://dist/index.html`，发布窗口是 `https://creator.douyin.com/...`
- 把 IPC 消息发到 `creator.douyin.com` 窗口，该页面没有我们的 preload，没有监听 `publish:statusChanged`
- 但 Chromium 的 WebFrameMain 在收到 `send` 时仍会尝试执行框架内的 JS
- 在某些时序下（页面恰好正在跳转）会触发 `Render frame was disposed`

这是一条反复踩坑后才稳定下来的关键防御逻辑。

---

## 九、发布流程中的状态同步机制

### 9.1 三条状态同步途径（互补而非替代）

```
途径 1: 前端登记
  Publish.vue submit()
    └─> publishStore.liveTasks[taskId] = { status: queued, progress: 0 }
  [目的：立即显示 UI，避免"提交后没反应"]

途径 2: 主进程主动推送
  PublishEngine.notifyStatus(task, verbose=true)
    └─> wc.send('publish:statusChanged', payload)
          └─> publishStore._applyStatusUpdate(raw)
                └─> Vue 响应式更新 → UI 刷新
  [目的：实时同步正在发生的进度]

途径 3: 前端主动拉取
  publishStore.submit() 末尾
    └─> electronApi.getPublishProgress(taskId)
          └─> 合并到 liveTasks
  [目的：防止第一次推送被遗漏时 UI 状态不一致]
```

### 9.2 任务完成后的自动清理

用户痛点：任务成功后"任务进行中"面板还一直显示着已完成的任务，页面显得混乱。

解决方案（`stores/publish.ts _applyStatusUpdate`）：

```typescript
// success → 8 秒后从 liveTasks 移除
// failed → 15 秒后移除（让用户有时间查看错误信息）
// running → 重新进入时清除之前的移除计划（防止从 failed 重试时被误删）

if (newStatus === 'success' || newStatus === 'failed') {
  const delay = newStatus === 'success' ? 8000 : 15000;
  if (this._autoRemoveTimers[taskId]) clearTimeout(this._autoRemoveTimers[taskId]);
  this._autoRemoveTimers[taskId] = setTimeout(() => {
    delete this.liveTasks[taskId];
    delete this._autoRemoveTimers[taskId];
  }, delay) as unknown as number;
} else if (this._autoRemoveTimers[taskId]) {
  clearTimeout(this._autoRemoveTimers[taskId]);
  delete this._autoRemoveTimers[taskId];
}
```

---

## 十、前端交互设计

### 10.1 账号管理页（AccountPanel.vue）

布局：左侧账号列表（el-table），右侧可选操作按钮。

```
┌─────────────────────────────────────────────────────────────┐
│  账号管理                                                    │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ [+] 新增账号    [刷新] [打开创作中心] [编辑] [删除]  │  │
│  │                                                       │  │
│  │ 平台 | 昵称 | 账号 | 粉丝 | 关注 | 获赞 | 状态 | 操作│  │
│  │ 抖音 | 用户A | 123 | 1000 | 200 | 5000 | 正常 | [...]│  │
│  │ 小红书 | 用户B | 456 | 12 | 16 | 62 | 正常 | [...]   │  │
│  │ ...                                                   │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

关键交互点：
- 账号卡片按 `capabilities` 过滤显示（"发布视频"的账号才会出现在视频发布页）
- "刷新" 按钮重新调用 `electronApi.refreshToken(id)`，从 DOM 重新提取最新数据
- "打开创作中心" 使用 `BrowserWindow` 独立窗口，partition 与账号一致

### 10.2 发布页（Publish.vue）

布局：左上发布表单，左下账号选择区，右侧任务进度面板（折叠面板展示），底部调试日志。

```
┌─────────────────────────────┬─────────────────────────────┐
│  发布内容                    │  任务进行中                  │
│  ┌─────────────────────────┐ │  ┌───────────────────────┐ │
│  │ 类型选择: [视频/图文]   │ │  │ task_xxx ... 85% ... │ │
│  │ 标题: [__________]      │ │  │ 子任务列表...        │ │
│  │ 内容: [__________]      │ │  └───────────────────────┘ │
│  │ 话题: tag1, tag2        │ │  ┌───────────────────────┐ │
│  │ 文件选择: [+ 选择文件]  │ │  │ [更多任务...]         │ │
│  │ 封面: [+ 选择封面]      │ │  └───────────────────────┘ │
│  └─────────────────────────┘ │                             │
│  账号选择（网格）            │  调试日志 (showDebug)        │
│  ┌────┐┌────┐┌────┐        │  ┌───────────────────────┐ │
│  │ 抖音││小红书││快手│        │  │ [INFO] 注入 cookies...│ │
│  └────┘└────┘└────┘        │  │ [WARN] ...             │ │
│                             │  └───────────────────────┘ │
└─────────────────────────────┴─────────────────────────────┘
```

### 10.3 响应式状态设计

核心响应式状态（stores/publish.ts）：
- `liveTasks: Record<taskId, LiveTask>` — 实时任务
- `_autoRemoveTimers: Record<taskId, number>` — 自动移除定时器句柄
- `_listenerInstalled: boolean` — IPC 事件监听器是否已安装（幂等保证）

---

## 十一、扩展指南

### 11.1 新增一个平台

只需在 `src/main/services/platforms/` 新增一个文件，文件内调用 `registerPlatform()` 即可被系统发现：

```typescript
// Step 1: 新增 platforms/example-platform.ts
import { runStandardPublish, evalJS } from './shared';
import { registerPlatform } from './registry';

const meta = {
  key: 'example',
  name: '示例平台',
  icon: '📝',
  publishUrl: 'https://creator.example.com/publish',
  homeUrl: 'https://creator.example.com/home',
  contentTypes: ['video', 'image'],
  capabilities: { publishVideo: true, publishImage: true, publishArticle: false },
  loginKeywords: ['我的内容', '发布作品'],
  // ...其他元信息
};

function detectLoggedIn(win) {
  return evalJS(win, `
    document.body.innerText.includes('我的内容') ||
    document.body.innerText.includes('发布作品')
  `, 'detect-login', log);
}

function publishVideo(accountId, request, onProgress) {
  return runStandardPublish(accountId, request, onProgress, {
    platform: 'example',
    meta,
    detectLoggedIn,
    publishKeywords: ['发布', '确认发布', '立即发布'],
    enableConfirmStep: true,
    enablePostClickVerify: true,
    fillWaitMs: 2000,
  });
}

// 统一注册（index.ts 会自动 import 本文件，注册在 registry 中）
registerPlatform({
  key: 'example',
  meta,
  capabilities: meta.capabilities,
  detectLoggedIn,
  publishVideo,
  publishImage: publishVideo,
});

export default { key: 'example', meta, capabilities: meta.capabilities, detectLoggedIn, publishVideo, publishImage: publishVideo };
```

**无需修改 PublishEngine / PlatformDispatcher** —— PlatformDispatcher 自动通过 `getPlatformAdapter()` 发现新平台。

### 11.2 新增一个内容类型（如"直播"）

1. 在 `types/index.ts` 的 `ContentType` 添加 `'live'`
2. 在平台 `meta.capabilities` 添加 `publishLive: true`
3. 在平台适配器实现 `publishLive()` 方法
4. 在 `PlatformDispatcher.ts` 的 switch 中添加 `case 'live'` 分支
5. 前端 Publish.vue 自动会根据账号 `capabilities.publishLive` 显示对应的账号卡片

### 11.3 调试技巧清单

| 问题 | 排查方法 |
|:---|:---|
| 发布成功但 UI 没显示 | 打开 Publish.vue → 切换 `showDebug` → 查看 `_applyStatusUpdate` 日志，检查是否收到了 IPC 推送 |
| 某个平台按钮点击没反应 | 在浏览器打开 `publishUrl` 模拟上传，检查发布按钮的 class/text，可能需要调整 `publishKeywords` |
| 标题/正文没填写 | 打开开发者工具，执行 `document.querySelector('textarea').value` 查看是否被 React 清空，可能需要调整选择器 |
| 粉丝/关注数显示为 null | 检查对应平台的 `extractPageInfo`，在 DevTools 中 `document.querySelector('.numerical')` 查看是否命中了数字元素 |
| 发布窗口没关闭 | 检查 `runStandardPublish` 成功分支，看是否走到了 `setTimeout(3000) → win.destroy()`；如果是失败分支，窗口不会自动关闭（设计如此） |
| "任务进行中"面板没消失 | 检查 `stores/publish.ts` 的 `_autoRemoveTimers` 是否被正确设置，任务 status 是否为 success/failed |

---

## 十二、已知问题与路线图

### 12.1 已解决的问题

| 问题 | 影响平台 | 解决方案 | 版本 |
|:---|:---|:---|:---|
| 图文发布标题填写后被清空（React 受控组件） | 抖音/小红书 | `execCommand('insertText')` 模拟原生输入，触发 React onChange | v1.1 |
| 小红书发布按钮在 closed shadow DOM 中无法点击 | 小红书 | CDP `DOM.getDocument(pierce:true)` + `Input.dispatchMouseEvent` 合成鼠标事件 | v1.1 |
| 快手图文发布页面 tabType 切换不正确 | 快手 | 直接使用 URL `?tabType=2` 进入图文模式 | v1.1 |
| 文件上传后 React 不感知文件变化 | 全平台 | `DOM.setFileInputFiles` 后手动派发 `change`/`input` 事件 | v1.1 |
| 标题/标签超过平台限制导致发布失败 | 全平台 | 发布前 truncate，日志记录超长内容 | v1.1 |

### 12.2 已知问题

| 问题 | 影响平台 | 现状 | 计划 |
|:---|:---|:---|:---|
| 小红书图文发布的封面选择器较复杂 | 小红书 | 部分场景可工作 | 改进 buildFillTitle 支持更复杂的图文上传 |
| 快手发布流程的检测登录逻辑依赖 textContent 关键字 | 快手 | 基本可工作 | 增加更精确的 DOM 选择器 |
| 定时发布功能无持久化恢复 | 全平台 | `scheduledAt` 存储到磁盘，但重启后不会恢复定时器 | 添加启动时扫描 scheduled 任务并恢复 |
| 自动更新未启用 | 全平台 | `electron-updater` 架构已预留，但未配置发布服务器 | 后续接入私有 OSS |

### 12.2 路线图

1. **v1.1 — 发布成功率提升**
   - 优化各平台按钮点击策略（多一次 DOM 扫描，使用更精确的选择器）
   - 引入 "人工介入" 机制：当自动发布失败时，允许用户在同一窗口手动点击，主进程重新检测是否成功
   - 增加发布历史页（从 `publishTasks` 读取，替换当前的"任务进行中"折叠面板）

2. **v1.2 — 数据中心**
   - 账号维度统计：各账号发布数、成功率、最近发布时间
   - 内容维度统计：标题长度/内容类型分布
   - 导出 Excel / JSON

3. **v1.3 — 插件平台**
   - `plugins/` 目录自动扫描
   - 每个插件实现 `PlatformAdapter` 接口即可被加载
   - 允许用户编写自定义平台适配脚本（如 B 站、微信公众号、知乎等）

---

---

## 十三、图文发布专章（v1.1 新增能力）

### 13.1 图文发布的价值

与视频发布的核心区别

图文发布（image content publishing），是 v1.1 新增的核心能力。与视频发布相比，主要差异如下：

| 维度 | 视频发布 | 图文发布 |
|:---|:---|:---|
| 上传内容 | `.mp4` / `.mov` 等视频文件 | `.jpg` / `.png` / `.webp` 等图片文件 |
| 上传 input | `accept="video/*"` | `accept="image/*"` |
| 富文本编辑 | 简单 contenteditable（描述信息 | ProseMirror / Tiptap 富文本编辑器 |
| 标题输入 | `<input type="text">` 或 contenteditable | `<input type="text">`（小红书除外） |
| 发布按钮 | 标准 DOM button/span/div | 小红书是 closed shadow DOM 内的 button |
| 字数限制 | 快手 500 / 抖音 1000+ | 小红书标题 20 / 正文 1000，快手描述 500 |
| 标签限制 | 抖音 10 个话题 | 快手最多 4 个标签 |
| URL 入口 | 平台默认发布页 | 需指定特殊参数切换到图文模式 |

### 13.2 三级分发：平台独立实现的图文发布

**设计原则**：每个平台的图文发布都是**独立代码路径**，完全隔离。修改一个平台绝不会影响其他平台。

```
PublishEngine.submit(req)
  │
  ├─> PlatformDispatcher.createExecutor(req.platform, 'image')
  │     │
  │     ├─> 小红书：xiaohongshu.publishImage()  ← 完全独立
  │     │     1. 打开窗口 + 导航跟踪
  │     │     2. 加载 https://creator.xiaohongshu.com/publish/publish
  │     │     3. uploadViaCDP(win, files, log, contentType='image')
  │     │     4. 填写标题（最多 20 字）
  │     │     5. ProseMirror 富文本填写正文
  │     │     6. CDP 穿透 shadow DOM 点击发布按钮
  │     │
  │     ├─> 抖音：douyin.publishImage()  ← 完全独立
  │     │     1. 打开窗口 + 导航跟踪
  │     │     2. 加载 https://creator.douyin.com/creator-micro/content/upload?default-tab=3
  │     │     3. uploadViaCDP(win, files, log, contentType='image')
  │     │     4. 填写标题（execCommand 避免被清空
  │     │     5. 填写正文描述
  │     │     6. 点击发布按钮.button-dhlUZE primary
  │     │
  │     └─> 快手：kuaishou.publishImage()  ← 完全独立
  │           1. 打开窗口 + 导航跟踪
  │           2. 加载 https://cp.kuaishou.com/article/publish/video?tabType=2
  │           3. uploadViaCDP(win, files, log, contentType='image')
  │           4. 不填写标题（快手图文无独立标题字段）
  │           5. 填写描述（最多 500 字）+ 标签（最多 4 个）
  │           6. 点击 _button-primary_ 发布
  │
  └─> shared.ts 仅提供通用工具（窗口 / 导航 / CDP 上传 / evalJS）
           ├─> makePublishWindow
           ├─> attachNavigationTracker
           ├─> uploadViaCDP(win, files, log, contentType)
           ├─> evalJS(win, code, desc, log)
           └─> cdpClickPublishButton(win, log, keywords)
```

### 13.3 uploadViaCDP 的 contentType 参数

`uploadViaCDP` 函数签名：

```typescript
export async function uploadViaCDP(
  win: BrowserWindow,
  files: string[],
  log: LogFn,
  contentType?: 'video' | 'image',  // ← 关键参数
): Promise<boolean>
```

内部逻辑：

1. CDP `DOM.getDocument({ depth: -1 }) 遍历 DOM 树
2. 收集所有 `input[type="file"]` 元素
3. **按 contentType 排序**：
   - `contentType='image'` 优先匹配 `accept` 属性含 `image/` 的 input
   - `contentType='video'` 优先匹配 `accept` 属性含 `video/` 的 input
   - 无 accept 或 accept='*/*' 的作为兜底
4. 跳过 `aria-hidden="true"` 容器内的隐藏 input
5. `DOM.setFileInputFiles(nodeId, files)` 注入文件
6. 手动派发 `change` + `input` 事件（让 React 感知到文件变化
7. 等待 `waitForUploadComplete` 轮询检测上传状态

**关键修复（小红书 ProseMirror 富文本编辑器

小红书的正文编辑区是 ProseMirror/Tiptap 富文本编辑器。

**为什么直接 `innerText` 不生效**：

```javascript
// ❌ 失败方案：
editable.innerText = content;
// ProseMirror 在下次 keydown 时还原 state，内容丢失

// ✅ 正确方案（在 xiaohongshu.ts buildFillContentScript 内实现
editable.focus();
document.execCommand('selectAll');
document.execCommand('delete');
// 逐行 insertText
document.execCommand('insertText', false, line + '\n');
// → ProseMirror 监听 input 事件
// → 更新内部 state.doc 正确更新
```

**为什么逐行插入**：ProseMirror 的 `insertText` 命令一次只能处理一行文本。多行内容需要分多次 execCommand 调用。

### 13.5 小红书发布按钮的 closed shadow DOM

小红书发布按钮的 DOM 结构：

```html
<xhs-publish-btn submit-text="发布笔记">
  <!-- #shadow-root (closed)  ← 外部 JS 无法访问
    <div class="publish-page-publish-btn">
      <button class="ce-btn bg-red">发布笔记</button>
    </div>
  </xhs-publish-btn>
```

**三层策略**：

1. **第一层：尝试 `element.click()` — 轻量级尝试点击 host 元素（有时平台会在 host 上也绑定事件
2. **第二层：CDP 穿透点击 — 使用 `DOM.getDocument({ pierce: true })` 强制穿透 shadow DOM
3. **第三层：坐标点击 — 对内部 button 的 `getBoxModel` 后，`Input.dispatchMouseEvent` 合成鼠标事件

```typescript
// cdpClickPublishButton 内部逻辑
1. CDP DOM.getDocument({ depth: -1, pierce: true })  ← 关键参数
2. 遍历所有候选元素的 nodeId
3. DOM.getBoxModel(nodeId) → content[0..3] / margin/padding/border
4. 计算中心坐标 (centerX, centerY)
5. Input.dispatchMouseEvent
   · type: 'mouseMoved' → 'mousePressed' → 'mouseReleased'
   · button: 'left'
   · clickCount: 1
6. 合成鼠标事件不受 shadow DOM 隔离，真正的按钮可接收到鼠标事件
7. 即使 JS click() 不生效时也能点击

### 13.6 字数与标签限制的统一约束

各平台的图文发布有严格的字数/标签限制，超过限制会导致发布失败。

| 平台 | 标题字数 | 正文字数 | 标签数 |
|:---|:---|:---|:---|
| 小红书 | **最多 20** | 最多 1000 | 话题最多 10 个 |
| 抖音 | 最多 80 | 最多 1000 | 最多 10 个话题 |
| 快手 | 无独立标题 | 最多 500 | 最多 **4** 个标签 |

**实现方式**：在每个平台的 `publishImage()` 内部，调用自己的 truncate 函数，而非 shared.ts 统一处理。这是平台差异隔离原则的体现。

```typescript
// 示例：小红书标题截断
const TITLE_LIMIT = 20;
const CONTENT_LIMIT = 1000;
const MAX_TAGS = 10;

function truncateText(text: string, limit: number): { text: string; truncated: boolean {
  if (!text) return { text: '', truncated: false };
  if (text.length <= limit) return { text, truncated: false };
  return { text: text.slice(0, limit - 1).trimEnd() + '\u2026', truncated: true };
}
```

超过限制时，会记录 warning 级别的日志，但不抛异常，继续后续流程。

### 13.7 图文 vs 视频的统一入口

前端 Publish.vue 的 contentType 切换：

```
用户选择 contentType = 'image' | 'video'
  │
  ├─> 前端校验：image 仅允许上传图片文件（扩展名白名单
  │
  ├─> 过滤账号：上传文件类型验证文件扩展名
  │
  └─> PublishEngine.submit(req)
        └─> PlatformDispatcher.createExecutor(platform, contentType)
              └─> adapter.publishVideo / publishImage （由 contentType 决定
```

PlatformDispatcher 的两级分发是整个系统的核心扩展性设计，后续新增「文章发布」等能力无需修改现有代码，只需在各平台 `publishArticle` 方法即可。

### 13.8 图文发布的平台技术文档

各平台图文发布的详细技术实现，都有独立文档：

| 文档 | 路径 | 说明 |
|:---|:---|:---|
| 小红书图文发布技术文档 | `docs/小红书图文发布技术文档.md` | ProseMirror / shadow DOM / 标题 20 字限制|
| 抖音图文发布技术文档 | `docs/抖音图文发布技术文档.md` | `default-tab=3` URL / React 受控组件 / execCommand |
| 快手图文发布技术文档 | `docs/快手图文发布技术文档.md` | `tabType=2` URL / 无独立标题 / 标签最多 4 个 |

---

## 十四、文章发布专章（v1.2 新增能力）

### 14.1 文章发布与图文/视频发布的区别

| 维度 | 视频发布 | 图文发布 | 文章发布 |
|:---|:---|:---|:---|
| 内容形式 | 视频文件 + 描述 | 图片 + 标题/正文 | 纯文字长文（富文本） |
| 素材文件 | 必填 | 必填 | **无** |
| 抖音入口 | `/upload` | `/upload?default-tab=3` | `/upload?default-tab=5` |
| 小红书入口 | 默认发布页 | 默认发布页 | `?source=official&from=menu&target=article` |
| 快手入口 | 支持 | 支持 | **不支持** |
| 标题限制 | 抖音 80，小红书 20 | 抖音 80，小红书 20 | 抖音 **30**，小红书 **64** |
| 正文限制 | 抖音 1000，小红书 1000，快手 500 | 抖音 1000，小红书 1000，快手 500 | 抖音 **8000**，小红书 **无限制** |
| 发布按钮路径 | 标准 DOM button | shadow DOM（小红书） | 小红书：3 步点击 → shadow DOM button<br/>抖音：标准 DOM button |
| 点击步数 | 1 步（点击"发布"） | 1 步（点击"发布"） | **3 步**（一键排版 → 下一步 → 发布） |

**核心区别**：小红书文章发布不是"填入 → 点击发布"的两步流程，而是**四步流程**——进入编辑器 → 填写 → 排版 → 下一步 → 发布。这是与图文发布最大的差异，需要独立的多步点击逻辑。

### 14.2 三级分发架构（扩展 contentType）

PlatformDispatcher 从之前的 2 级（video / image）扩展为 3 级（video / image / article）：

```typescript
// PlatformDispatcher.createExecutor
export function createExecutor(
  platform: string,
  contentType: 'video' | 'image' | 'article',  // ← 新增 'article'
): PublishExecutor

// 每个平台 adapter 接口扩展
interface PlatformAdapter {
  publishVideo(accountId, request, onProgress)      // 视频发布
  publishImage(accountId, request, onProgress)      // 图文发布
  publishArticle(accountId, request, onProgress)    // 文章发布（v1.2 新增）
  detectLogin(accountId)
  refreshAccount(accountId)
}

// 平台 meta 扩展
interface PlatformMeta {
  contentLimits: { title?: number; content?: number }  // 图文/视频限制
  articleLimits: { title?: number; content?: number }  // 文章限制（独立配置）
  contentTypes: Array<'video' | 'image' | 'article'>   // 支持哪些类型
}
```

**分发逻辑**：`createExecutor(platform, 'article')` → 调用 `adapter.publishArticle`
- 抖音/小红书：`publishArticle` 已实现，返回 `PublishItemProgress`
- 快手：`publishArticle` 不存在，PlatformDispatcher 在前端就会过滤掉（根据 `contentTypes` 能力）

**前端过滤**：用户选择「文章」类型时，Publish.vue 只显示 `meta.contentTypes.includes('article')` 的账号 → 快手账号自动隐藏。

### 14.3 抖音文章发布实现要点（封面上传已解决）

```
┌──────────────────────────────────────────────────────┐
│  publishArticle(douyin.ts)                            │
│                                                      │
│  1) 打开 URL: /creator-micro/content/upload?         │
│     default-tab=5  ← 唯一的文章发布入口              │
│                                                      │
│  2) 检测引导页并点击"我要发文"按钮                    │
│     → semi-button semi-button-primary                │
│                                                      │
│  3) 填写标题（30 字限制）                            │
│     buildFillTitle(title, 30)                        │
│     → execCommand('insertText') 逐字输入              │
│     → React onChange 触发                             │
│                                                      │
│  4) 填写正文（8000 字限制）                          │
│     buildFillContent(content, 8000)                  │
│     → 自动分段写入富文本编辑器                       │
│                                                      │
│  5) 等待自动保存草稿（2 秒）                         │
│     sleep(2000)                                      │
│                                                      │
│  6) 🔑🔑🔑 上传封面图（核心难点，已解决）            │
│     uploadArticleCover(win, coverFile, log)          │
│     → 5步流程 + 五层拦截机制                         │
│     → JS层DataTransfer直接注入 + CDP兜底             │
│     → 详见 docs/抖音文章发布技术文档.md 第三节        │
│                                                      │
│     五层防护架构：                                    │
│       Layer 1: CDP Page.setInterceptFileChooserDialog│
│                （浏览器层面拦截原生对话框）           │
│       Layer 2: pointerdown/mousedown/click 捕获阶段   │
│                preventDefault() + 立即设置文件        │
│       Layer 3: HTMLInputElement.prototype.click() 拦截│
│                重写click()直接设置文件，不弹窗        │
│       Layer 4: MutationObserver监听DOM变化           │
│                动态input出现立即设置文件              │
│       Layer 5: showOpenFilePicker()拦截              │
│                File System Access API兜底            │
│       CDP兜底：3秒后降级DOM.getDocument(pierce:true)  │
│                                                      │
│  7) 点击发布按钮                                     │
│     button.button-dhlUZE.primary-button              │
│     → 与图文发布共享的发布按钮                        │
│                                                      │
│  8) 等待发布结果（60 秒超时）                        │
│     检测 URL 离开 upload 页 → 成功                   │
│                                                      │
│  限制常量（独立于图文/视频）：                       │
│    ARTICLE_TITLE_MAX   = 30                          │
│    ARTICLE_CONTENT_MAX = 8000                        │
└──────────────────────────────────────────────────────┘
```

### 14.4 小红书文章发布实现要点（核心：多步点击）

```
┌──────────────────────────────────────────────────────┐
│  publishArticle(xiaohongshu.ts)                       │
│                                                      │
│  1) 打开 URL: /publish/publish?target=article        │
│     → 默认显示文章管理列表，不是编辑器               │
│                                                      │
│  2) 🔑 检测页面状态，点击"新的创作"进入编辑器        │
│     hasEditor && hasTitleInput → 已在编辑器，跳过    │
│     otherwise → 点击 button.bg-red（"新的创作"）     │
│     → waitForStable + sleep(2000)                    │
│                                                      │
│  3) 填写标题（64 字限制）                            │
│     buildFillTitle(title, 64)                        │
│     → ProseMirror 富文本标题区域                     │
│                                                      │
│  4) 填写正文（无字数限制）                           │
│     buildFillContent(content, null)                  │
│     → null 表示无字数限制                            │
│     → ProseMirror 正文编辑器                         │
│                                                      │
│  5) 🔑🔑🔑 三步点击发布流程（核心）                 │
│                                                      │
│     第 1 步：点击"一键排版" button.ce-btn.bg-red     │
│       → 轮询等待按钮文本变化（变成"下一步"）         │
│       → 每秒检查一次，10 秒超时                       │
│       → 降级策略：JS querySelector 10 次失败         │
│                   → CDP 穿透 shadow DOM 点击         │
│                                                      │
│     第 2 步：点击"下一步"                            │
│       → 同一 button.ce-btn.bg-red，文本已变为"下一步"│
│       → waitForStable(5000ms) + 检查 URL             │
│                                                      │
│     第 3 步：点击"发布"                              │
│       → 按钮在 closed shadow DOM 中: <xhs-publish-btn>│
│       → CDP 深度扫描（pierce:true）                  │
│       → Input.dispatchMouseEvent 合成真实鼠标点击    │
│                                                      │
│  6) 发布结果检测                                    │
│     新增专用 URL 检测：/publish/success → 成功       │
│     新增专用 URL 检测：published=true → 成功          │
│     与原有（图文/视频发布）的 URL 检测互补            │
│                                                      │
│  限制常量（独立于图文/视频）：                       │
│    PUBLISH_ARTICLE_URL = ...target=article           │
│    ARTICLE_TITLE_LIMIT = 64                          │
│    ARTICLE_CONTENT_LIMIT = undefined（无限制）       │
└──────────────────────────────────────────────────────┘
```

### 14.5 代码隔离原则

文章发布的代码完全不影响图文/视频发布：

| 模块 | 代码隔离方式 |
|:---|:---|
| **`publishArticle` 方法** | 每个平台文件中独立函数体，不调用 `publishImage` 或 `publishVideo` 的任何逻辑 |
| **限制常量** | 文章有独立的 `ARTICLE_*_LIMIT` 常量，不与 `TITLE_LIMIT` / `CONTENT_LIMIT` 共用 |
| **发布结果检测** | `buildPublishResultProbeScript` 新增 `isArticleSuccess` 分支，不修改原有逻辑 |
| **按钮点击逻辑** | 小红书文章发布完全走独立的三步点击函数，不调用图文发布的 `click-publish` 代码 |
| **meta 配置** | `articleLimits` 与 `contentLimits` 是两个独立字段，前端 `Publish.vue` 按 contentType 选择读取 |
| **前端 UI** | 发布页面的「视频 / 图文 / 文章」三个 Tab 独立渲染，文章 Tab 下快手账号自动过滤 |

> **修改文章发布的任何逻辑都不影响图文/视频发布**。反之亦然。

### 14.6 平台能力矩阵

```
                视频     图文     文章
               (video)  (image)  (article)
┌──────────┬────────────────────────────┐
│ 抖音     │    ✅       ✅       ✅     │
│ 小红书    │    ✅       ✅       ✅     │
│ 快手     │    ✅       ✅       ❌     │
└──────────┴────────────────────────────┘
```

| 平台 | `publishVideo` | `publishImage` | `publishArticle` | `contentTypes` meta |
|:---|:---:|:---:|:---:|:---|
| 抖音 | ✅ | ✅ | ✅ | `['video', 'image', 'article']` |
| 小红书 | ✅ | ✅ | ✅ | `['video', 'image', 'article']` |
| 快手 | ✅ | ✅ | ❌ | `['video', 'image']` |

### 14.7 文章发布技术文档索引

| 平台 | 文档 | 关键信息 |
|:---|:---|:---|
| 抖音 | `docs/抖音文章发布技术文档.md` | `default-tab=5` URL / 标题 30 字 / 正文 8000 字 / React 受控组件 |
| 小红书 | `docs/小红书文章发布技术文档.md` | `target=article` URL / 三步点击（一键排版 → 下一步 → 发布） / 标题 64 字 / 正文无限制 / shadow DOM 穿透点击 |

---

本文档基于实际代码实现撰写（对应 commit: 基于 Electron 31.7.7 的完整实现），可作为团队协作和后续维护的依据。

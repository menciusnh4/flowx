# FlowX Desktop —「账号健康诊断」产品需求文档（PRD）与实施评估

> 版本：v0.2（决策锁定版） · 日期：2026-07-29 · 作者：产品通（基于《技术实施方案 v0.1.1》+ 现有代码库现状整理）
> 关联文档：《FlowX_账号健康诊断_技术实施方案.docx》（技术预研版）
> **决策状态**：5 项开放问题已于 2026-07-29 全部对齐（见 §9），方向锁定 = 砍 Python 栈、复用现有 TS 采集、平台内基准、违规维度小范围试点、僵尸粉代理估算、独立导航入口、MVP 仅应用内触达。

---

## 0. 一句话定位

把"我的号有没有问题"这个小白用户最根本的前置问题，变成一个 **0–100 分 + 红绿灯 + 人话解读** 的账号级健康度产品，让矩阵运营从"出了事才知道"变成"提前 48–72 小时预警"。

---

## 1. Why（为什么做）— 用户问题与业务目标

### 1.1 用户问题
- **前置问题无人回答**：当前 FlowX v0.1.1 集中在"发布执行层"，用户无法在发内容前知道"我的号现在健不健康"。
- **风险来得太晚**：行业数据显示企业矩阵账号年均封禁率 >35%，单次封禁平均损失 >10 万元；其中 **82% 的风险是可提前识别的异常信号**，而非突发。
- **专业指标看不懂**：完播率、互动率、信用分等指标对小白用户是天书，缺"翻译"。

### 1.2 业务目标
| 目标 | 可衡量口径 |
|------|-----------|
| 降低矩阵封禁损失 | 接入诊断后 90 天内，因"可识别异常未处理"导致的封禁数下降（基线待采集） |
| 提升账号资产可观测性 | 已绑定账号中"完成过一次健康诊断"的覆盖率 ≥ 80% |
| 建立差异化卖点 | "小白友好 + 红绿灯"成为 vs 易媒助手等竞品的核心差异点 |
| 复用现有能力降本 | 数据采集 70% 复用已合并的 `账号分析` 模块，不引入第二套采集栈 |

---

## 2. 目标用户与使用场景

- **主用户**：矩阵运营小白 / 初级运营（核心价值接收者，要"人话 + 红绿灯"）。
- **次用户**：中高级运营 / 风控（要批量扫描、维度明细、异常时间线）。
- **场景**：① 发内容前自检；② 每周例行批量体检；③ 收到预警后定位根因并整改；④ 给老板出账号资产健康周报。

---

## 3. 用户故事（标准格式）

- 作为**小白运营**，我希望打开一个账号就能看到"健康分 + 红绿灯 + 这段话在说什么"，以便不用懂指标也能知道号有没有问题。
- 作为**矩阵运营**，我希望一次扫描全部绑定账号并按严重程度排序，以便优先处理最危险的号。
- 作为**运营**，我希望系统在我粉丝骤降/互动率暴跌时提前预警，以便赶在限流/封号前整改。
- 作为**风控**，我希望看到每个扣分项"当前值 vs 建议值"，以便给账号主具体的整改动作。
- 作为**管理者**，我希望导出账号健康周报，以便向上汇报矩阵资产状态。

---

## 4. 功能范围

### 4.1 Go（纳入本期）
1. **账号健康度总评分卡**：0–100 分 + 颜色圆环 + 5 级健康等级（🟢优秀/🟢良好/🟡一般/🟠预警/🔴高危）。
2. **五维雷达图**：基础信息完整度 / 账号活跃度 / 违规与安全 / 粉丝质量 / 内容表现力。
3. **近 30 天健康度趋势线**：标注关键事件（违规、异常登录、掉粉拐点）。
4. **问题清单**：按严重程度排序，每项含「扣分指标 → 当前值 → 建议值 → 操作入口」。
5. **小白解读器**：专业指标 → 人话 + 红绿灯 emoji。
6. **异常检测预警**：粉丝异常波动、互动率骤降、操作行为异常、违规信号突增 4 类。
7. **批量诊断入口**：一键扫描全部绑定账号，列表按风险等级排序。
8. **数据采集**：基于现有 `账号分析` 采集管线扩展至账号级指标（详见 §8）。
9. **独立导航入口**：新增一级菜单「**账号诊断分析**」，与「账号分析」同级、独立打开；入口直达健康度总览 + 五维 + 批量诊断（不嵌套进「账号分析」子页）。

### 4.2 Non-goals（不纳入，防范围蔓延）
- **不重复建设作品级诊断**：作品级 hook/content/engagement 诊断已由合并的 `账号分析` 模块（`WorkDiagnosis`）覆盖，本期只做**账号级**维度，不碰单作品质量评分。
- **不引入 Python/MediaCrawler/Playwright 第二采集栈**（详见 §8 可行性，这是本期最大纠偏点）。
- **不做深度学习异常检测（Autoencoder）**：明确列入 V2，本期只用 Z-Score / 移动平均残差 / Isolation Forest 等价算法。
- **不做云端/SaaS 化**：本期纯本地（SQLite），MySQL/PostgreSQL 团队协作为 V2 可选。
- **不做自动整改执行**：本期只"给建议 + 一键跳转创作中心/对应页面"，不自动改账号设置（避免误操作与合规风险）。
- **不做政治/敏感内容相关判定**：仅做中性运营健康度指标。

---

## 5. 核心功能需求（按模块）

### 5.1 数据采集层（复用 + 扩展）
- **复用**：现有 `src/main/services/analytics/platforms/*Collector.ts`（抖音/快手/小红书/微信视频号）已通过 Electron `<webview>` + 登录态 `safeEval` 采集 粉丝数/获赞/评论/完播率 —— **直接复用**，不重写。
- **扩展**（新增账号级指标）：
  - 基础信息完整度：头像/昵称/简介/认证状态/运营天数（来自账号资料，易得）。
  - 活跃度：近 30 天发布数、登录频率、互动频率（部分来自现有采集 + 本地行为日志 `src/main` 日志）。
  - **违规与安全（MVP 小范围试点）**：仅采集「异常登录设备 / 处罚等级」**若后台页面可稳定采集**；其余违规明细（创作者后台完整违规记录）暂缓。不可采的账号，该维度标记为「数据不足」而非给 0 分。
  - 粉丝质量：粉丝增长率、掉粉率（可得）；**僵尸粉比例（高不确定性，需平台 API 或启发式估算，见 §8 风险）**。

### 5.2 评分引擎（净新增，TS 原生）
- 五维加权模型（权重初值：基础 20% / 活跃 20% / 违规 30% / 粉丝质量 15% / 内容表现 15%）。
- Min-Max 归一化到 [0,100]；**行业基准 Xmax/Xmin 来源 = 平台内同类型账号统计**（聚合本产品已采集的同类账号快照，分平台/分层级计算分位），非外部行业均值（决策 1）。
- AHP 权重可配置（按平台/行业动态调整，如强监管领域违规权重可提到 40%）。
- 复用现有 `AnalyticsStore`（SQLite + `autoCollectIntervalHours:6`）存储时间序列，新增健康历史表（≥180 天）。

### 5.3 异常检测（净新增，TS 原生优先）
- Z-Score（|Z|>3）、移动平均+残差（窗口 7–14 天，阈值 2–3σ）、STL 季节性分解（互动周期）、Isolation Forest 等价（多变量联合）。
- 降误报三策略：灵敏度控制（contamination 2%–5%）、持续性要求（连续 ≥3 点异常）、集成投票（2/3 同意）。
- **MVP 宁可漏报不可误报**。

### 5.4 可视化与解读（复用前端栈）
- 复用现有 `AnalyticsPanel.vue` 作为容器模板；图表用 **ECharts 5**（雷达/趋势/对比），评分卡用 Canvas/HTML+CSS 圆环。
- 诊断报告页布局（技术方案 6.1）：顶部总评分卡 → 中部左雷达 + 中部右趋势 → 下部问题清单 → 底部一键解读。
- 红绿灯标签、人话解读器（技术方案 6.2 翻译表直接落地）。

### 5.5 预警与触达
- 应用内通知（账号列表角标 + 系统通知）；本期不做短信/邮件/IM 推送（V2）。
- 预警可一键跳转对应账号诊断详情与整改建议。

---

## 6. 指标体系（北极星 + 驱动 + 健康）

### 6.1 北极星指标
**账号健康度覆盖率 × 预警有效触达率**
= （近 30 天至少完成 1 次诊断的绑定账号数 / 总绑定账号数）×（触发预警且用户在 72h 内查看的比例）
> 含义：产品价值 = 让更多号被持续监测 + 预警真的被人看到。

### 6.2 驱动指标（分维度）
| 维度 | 驱动指标 | 基线/目标（示例，待定） |
|------|---------|------------------------|
| 数据采集 | 单账号诊断平均耗时 | < 30s（批量 < 5min/100 账号） |
| 评分 | 五维评分产出准确率（人工抽检） | ≥ 90% 与专家判定一致 |
| 异常 | 预警精确率（Precision） | MVP ≥ 70%，V2 ≥ 85% |
| 异常 | 预警召回率（Recall） | MVP ≥ 40%（宁可漏报），V2 ≥ 70% |
| 行为 | 诊断后 7 日内整改动作发起率 | ≥ 30% |

### 6.3 健康指标（防功能本身跑偏）
- 误报率（用户标记"误报"占比）≤ 10%。
- 采集成功率（各平台账号级指标完整率）≥ 85%；违规/僵尸粉维度允许更低并标注"数据不足"。
- 性能：诊断页首屏 < 1.5s（缓存历史分）。

---

## 7. 阶段计划（产品路线图，4 阶段）

> 对齐技术方案八章 MVP，但从"用户价值递进"重新排布；每阶段都有可独立上线的用户价值。

| 阶段 | 周期(估) | 交付物 | 验收标准 | 依赖/风险 |
|------|---------|--------|---------|----------|
| **P1 数据管道** | 2–3 周 | 复用现有 Collector + 扩展账号级指标（基础信息/活跃度/粉丝增长）；SQLite 健康历史表；违规维度采集原型 | 8 平台账号基础数据稳定入库；单账号可出原始指标快照 | 违规/僵尸粉采集可行性（中等） |
| **P2 评分引擎** | 2 周 | 五维加权评分 + AHP 权重配置 + 5 级分级；总评分卡 + 雷达图 UI | 输入原始数据正确输出 0–100 分与等级；权重可在配置页调整 | 行业基准 Xmax/Xmin 需 seeded（先用平台内同类型账号统计） |
| **P3 异常检测** | 2–3 周 | Z-Score + 移动平均残差 + 多变量孤立森林等价；预警规则配置；趋势线事件标注 | 能识别粉丝骤降/互动率暴跌/操作异常；连续 3 点 + 集成投票触发 | TS 实现 Isolation Forest 等价需验证效果 |
| **P4 可视化与批量** | 2–3 周 | ECharts 诊断报告页 + 小白解读器 + 批量诊断入口 + 应用内预警通知 | 用户看到完整 评分卡+雷达+问题清单+人话建议；一键批量扫描排序 | 依赖 P1–P3 |

**总体估时 8–11 周**（≈ 2–2.5 个月），与技术方案一致。建议 P1/P2 先内部灰度（自己矩阵账号），P3 起小范围公测。

---

## 8. 可实施性评估（价值判断官视角 — 重点）

### 8.1 总体结论：**可行，但技术实施方案需做一处重大纠偏**
技术方案质量高、模型与可视化设计成熟；但它在"数据采集层"按**绿地（greenfield）**假设选了 **Python + MediaCrawler + Playwright + stealth** 栈，**与 FlowX 真实架构严重错配**，且忽略了本仓库已合并的 `账号分析` 模块。纠偏后可行性高。

### 8.2 与现有 `账号分析` 模块的真实关系（最关键发现）
- 已合并模块 = **作品级诊断**（`WorkDiagnosis`：hook/content/engagement/conversion/clickThrough，单作品质量分），并自带 **抖音/快手/小红书/微信视频号 4 平台 Collector**（通过 Electron `<webview>` + 登录态 `safeEval` 采 粉丝/获赞/评论/完播率）。
- 本功能 = **账号级健康诊断**（5 维度，单账号）。
- **结论**：两者是**互补而非竞争**——共享采集管线、评分模型不同。健康诊断应**在现有 Collector 上扩展账号级指标**，而非另起 Python 采集栈。
- 这直接砍掉技术方案里"通道 B Playwright + stealth.min.js"的大部分工作量与风险，并把采集成功率从"新写"变为"扩展已验证代码"。

### 8.3 架构现实对照
| 技术方方案假设 | FlowX 现实 | 判断 |
|---------------|-----------|------|
| Python 主后端（MediaCrawler/scikit-learn/scipy） | Electron + Node 主进程 + Vue/TS 渲染；已有 analytics 用 TS 采集 | **不引入 Python 后端**；评分/异常用 TS 原生实现 |
| Playwright + stealth 规避检测 | 已有 `<webview>` + 登录态采集（本会话刚修复登录态） | **复用 webview 登录态**，更稳且 ToS 风险更低 |
| 本地 SQLite + 可选 MySQL | 已有 `AnalyticsStore` SQLite + 6h 定时 | **复用现有存储与调度** |
| ECharts 5 可视化 | 前端已用 Element Plus；AnalyticsPanel 可作模板 | **复用** |
| 6 层架构 | 桌面应用分层可简化 | 保留"采集→评分→异常→可视化"4 层即可，不必硬套 6 层 |

### 8.4 算法可行性（TS 原生足够）
- Z-Score、移动平均残差、STL 分解、Min-Max 归一化、AHP 特征值求解：**均可在 TS 用轻量数学库（如 mathjs / jstat）或自实现**，无需 Python。
- Isolation Forest：**TS 无成熟等价库**。两条路——① 复用现有 analytics 已引入的 Python 能力（若有）仅跑此算法；② 用"多变量 Z-Score + 规则组合"近似，MVP 够用。建议 MVP 用近似，V2 再评估是否嵌轻量 Python 子进程。

### 8.5 数据可行性（分维度）
| 维度 | 数据可得性 | 说明 |
|------|-----------|------|
| 基础信息完整度 | ✅ 高 | 账号资料直接可得 |
| 账号活跃度 | ✅ 高 | 发布记录 + 本地行为日志 |
| 内容表现力 | ✅ 高 | 复用现有 完播率/互动率采集 |
| 违规与安全 | ⚠️ 中（试点缩围） | MVP 仅采「异常登录/处罚等级（若后台可采）」；其余违规明细暂缓。不可采标记「数据不足」。决策 2 |
| 粉丝质量（僵尸粉比例） | 🟡 代理可接受 | 无平台 API 难获真值；**决策 3 接受"掉粉率 + 互动/粉丝比"代理指标 + 标注"估算"**，不强行给绝对值 |

### 8.6 合规与平台风险（必须前置处理）
- **隐私合规**：手机号、Cookie 等敏感数据本地加密（现有 `setupImageAntiHotlink`/加密约定需沿用），不上云 —— 与技术方案九章一致。
- **平台 ToS**：用 stealth 规避检测属高危（讽刺的是可能诱发封号，与产品目标相悖）。复用 webview 登录态采集显著更安全；仍须遵守各平台 robots/Rate Limit，采集频率控制在低峰。
- **误报治理**：MVP 明确"宁可漏报不可误报"，避免错误预警导致用户误停账号造成损失。

### 8.7 推荐落地路径（纠偏后）
1. **砍掉 Python/MediaCrawler/Playwright 采集栈**，改为扩展现有 TS Collector（省 30%+ 工作量与全部部署复杂度）。
2. 评分 + 异常检测 **TS 原生实现**，Isolation Forest 用近似算法兜底。
3. 存储/调度/可视化 **全量复用** 现有 analytics 基础设施。
4. 违规维度与僵尸粉比例作为"数据不足可标注"的柔性维度，避免为凑数据踩合规红线。
5. 前端以 `AnalyticsPanel.vue` 为骨架快速出诊断报告页。

---

## 9. 决策记录（2026-07-29 stakeholder 已对齐，全部锁定）

| # | 开放问题 | 决策 | 落地点 |
|---|---------|------|--------|
| 1 | 行业基准 Xmax/Xmin 来源 | **平台内统计**：用本产品已采集的同类账号快照分平台/分层级计算分位（非外部行业均值） | §5.2 评分引擎、T1.6 基准统计任务 |
| 2 | 违规维度是否纳入 MVP | **小范围试点**：仅采「异常登录/处罚等级（若后台可采）」；其余违规明细暂缓；不可采标记「数据不足」而非 0 分 | §5.1、T1.3 违规试点采集 |
| 3 | 僵尸粉比例真值 vs 代理 | **暂接受代理指标 + 标注估算**：用"掉粉率 + 互动/粉丝比"代理，前端明确标注"估算" | §5.1、§8.5、T2.1 评分引擎 |
| 4 | 与「账号分析」导航区隔 | **独立入口**：新增一级菜单「账号诊断分析」，与「账号分析」同级、独立打开（非子页） | §4.1-9、T4.3 导航入口 |
| 5 | 推送渠道 | **MVP 仅应用内**（账号列表角标 + 系统通知）；短信/邮件/IM 列入 V2 | §5.5、T3.5 应用内触达 |

> 五项均已拍板，无遗留 blocker。后续若试点发现「异常登录/处罚等级」后台采集普遍不可行（决策 2 的"若"不成立），则 MVP 违规维度整体降级为「数据不足」占位，不阻塞上线。

---

## 10. 附：与技术方案的差异速查
- 采集栈：方案=Python/MediaCrawler → 建议=复用现有 TS webview Collector。
- 架构层数：方案=6 层 → 建议=4 层（采集/评分/异常/可视化）。
- 作品 vs 账号：方案未区分 → 明确账号级，作品级已由 `账号分析` 覆盖。
- 深度学习：方案=V2 → 维持 V2，但 Isolation Forest 在 MVP 用近似。
- 合规：方案已提及 → 强化"复用登录态优于 stealth"，降低 ToS 风险。
